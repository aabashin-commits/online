# План реализации лендинга «Моя школа LS» (макет: 1920.png)

## ⚠️ Это ОНЛАЙН-версия — читать первым делом

Папка `скул онлайн/` создана 12.08.2026 копией офлайн-лендинга
(`скул офлайн/`, бывшая `school/`). Дальше папки живут **независимо**: общих файлов
нет, правки друг в друга не переносятся. Весь план ниже писался под офлайн-школу —
он остаётся историей вёрстки блоков и по-прежнему верен по геометрии, но всё
городское в нём **неактуально**: городов, филиалов, адресов и генератора
`build-cities.py` в этой папке нет. Что именно переделано — таблица в
`CLAUDE.md`, раздел «🌐 Онлайн-версия».

### Открытые вопросы к клиенту (не закрыты на 12.08.2026)

Клиент решил домен, телефон, CRM-форму и счётчики скопировать с офлайн-сайта как
есть. Это временно, перед запуском нужно получить и подставить:

1. **Домен онлайн-школы.** Сейчас во всех абсолютных URL стоит
   `agb.lancmanschool.com` — тот же, что у офлайн-сайта. Пока так, две разные
   страницы канонизируются на один адрес и в индексе останется одна.
2. **ID CRM-формы Битрикс24 для онлайн-школы.** Сейчас `click/225/yn061r` +
   `loader_225.js` — московская форма с выбором филиала из трёх адресов.
   Менять в `index.html` в двух местах (лоадер и `.b24-trigger`).
3. **Телефон онлайн-школы** — сейчас `+7 (499) 346-73-50` (шапка, бургер, футер).
4. **Отдельные счётчики Roistat и Яндекс.Метрики** — сейчас общие с офлайн-сайтом,
   статистика двух сайтов складывается в одну.
5. **Email для футера** — реального адреса у нас не было, выдумывать не стали;
   в блоке контактов сейчас Telegram, WhatsApp и ВКонтакте (настоящие).
6. **Доступы к площадке онлайн-школы** — в `.vscode/sftp.json` лежат креды
   офлайн-сайта, поэтому `uploadOnSave` **выключен**. Включать только после замены.

### Что ещё стоит показать клиенту

- Тексты всех блоков переписаны нами под онлайн-формат (уроки в прямом эфире,
  запись занятий, куратор, самоподготовка вместо ДЗ на вечер, экстернат) —
  **нужна вычитка клиентом**, как и в офлайн-версии.
- Распорядок дня начальной школы в блоке «Программы» придуман по аналогии
  с офлайн-расписанием (утренний круг, уроки в эфире, динамическая пауза,
  самоподготовка с куратором). Реальный онлайн-распорядок — у клиента.
- Фото и видео переиспользованы из офлайн-съёмки по решению клиента: в слайдере
  «О нас» остался ролик «1 сентября в школе», в отзывах — те же 15 роликов
  учеников очной школы.

## Правила проекта (переопределены, важнее общих правил репозитория)

- **Стек:** чистые HTML + CSS + vanilla JS. Никаких фреймворков (Nuxt/React/Vue), сборщиков и Tailwind.
- **Файлы по блокам:** у каждого блока свой CSS-файл; JS-файл — у блоков с поведением. Подключение напрямую в `index.html` (`<link>` + `<script defer>`), без сборки.
- **CLAUDE.md внутри `school/`** — описание проекта и файловой структуры (создаётся первым шагом реализации, поддерживается актуальным).
- **`school/фото тз/`** — сюда клиент кладёт макеты: цвета, шрифты, состояния, мобилка. Перед вёрсткой каждого блока сверяюсь с содержимым папки.

## Файловая структура (целевая)

```
school/
├── CLAUDE.md            — правила и структура проекта
├── PLAN.md              — этот план
├── фото тз/             — макеты от клиента (цвета, состояния, мобилка)
├── index.html           — главная (Москва) = источник правды
├── gelendzhik.html      — версия Геленджика (генерируется build-cities.py, не править руками)
├── build-cities.py      — генератор gelendzhik.html из index.html (в игноре sftp.json)
├── assets/
│   ├── fonts/           — Onest variable (woff2, сабсеты cyrillic/latin/latin-ext, OFL.txt)
│   ├── img/             — по подпапкам блоков: hero/, attestat/, about/, ...
│   └── video/           — ролики отзывов (если mp4, а не эмбеды)
├── css/
│   ├── base.css         — reset, CSS-переменные (цвета/шрифты/радиусы), типографика
│   ├── layout.css       — контейнер, общие компоненты: кнопки, пиллы, бейджи, заголовки секций
│   ├── header.css   hero.css   info-cards.css   attestat.css
│   ├── classes.css  about.css  universities.css pricing.css
│   ├── ege-banner.css   programs.css   no-homework.css   staff.css
│   ├── invite.css   reviews.css   faq.css   electives.css
│   ├── footer.css   form.css
└── js/
    ├── main.js          — инициализация, общие хелперы (smooth scroll, лайтбокс-каркас)
    ├── header.js        — бургер-меню, поведение при скролле
    ├── programs.js      — табы расписания: данные 3 программ + рендер сетки
    ├── staff.js         — слайдер персонала (категории × люди, миниатюры, стрелки)
    ├── reviews.js       — видео: открытие/проигрывание
    ├── faq.js           — аккордеон
    └── form.js          — маска +7, валидация, отправка, состояния, модалка успеха
```

CSS-переменные в `base.css` заполняются из макетов цветов, которые клиент положит в «фото тз». Классы — БЭМ-подобные с префиксом блока (`.hero__title`, `.staff__thumb--active`).

## Блоки: реализация и что нужно

Формат: **блок → файлы → что делаю → что жду из «фото тз» / от клиента**.

1. **Шапка** — `header.css`, `header.js`. ✅ Свёрстана: на десктопе только 7 якорей на всю ширину (по скрину клиента, CTA и лого в шапке нет); ≤1280 — лого + бургер-меню по `360 меню.png`. ❗Решение клиента: «Кейсы» и «Преимущества» удаляем из футера; «Преимущества» из шапки убираю по той же причине (секции нет). Жду: десктопный макет шапки (в `Десктоп целыый.png`/`1920.png` её нет — экспорт начинается с блока «Программы»), липкость при скролле, ховеры пунктов (пока синий).
2. **Hero** — `hero.css`. ✅ Десктоп свёрстан по скрину клиента (17.07.2026): панель с градиентом + сетка `hero/hero-line.svg`, пиллы локаций (`map-pin.svg`), подсветка в H1 (CSS, color-mix), CTA + стикер, дети `hero/kid.webp` (2x 1630px, 135КБ; исходник `hero-kid.png` 5.7МБ — можно удалить после приёмки). В вёрстке «Гостиный двор» (в макете опечатка «Гостинный»). Жду: мобильный макет (❗«Мобайл целый.png» без hero — начинается с тарифов; адаптив пока приблизительный), подтверждение года набора (в вёрстке 2025/26 как в макете, сейчас июль 2026 → вероятно 2026/27), подтверждение «Гостиный».
3. **Инфо-карточки (4 шт.)** — `info-cards.css`. ✅ Свёрстаны по скрину клиента: ряд шириной с панель hero (20px от краёв), наезд на hero через `--overlap: 110px`; высота панели hero поднята до 695px по макету (см. комментарий в `hero.css`) — CTA и стикер не перекрываются. ❗Опечатка макета «пристижные» в заголовке карточки 2 исправлена на «престижные» — согласовать с клиентом. На секции временный `id="advantages"` для пункта шапки «Преимущества».
4. **Аттестат** — `attestat.css`. ✅ Свёрстан по скрину клиента: белая карточка (`overflow:hidden`, волна клипается по скруглению), пиллы-лента во всю ширину (`space-between`), подсветка-маркер части абзаца (`box-decoration-break`), 2 баббла «гос.образец»/«лично в руки», коллаж `attestat/diplom.webp` (из PNG 9.3МБ → 247КБ) + волна `attestat/diplom_figura.webp` из правого-верхнего угла за коллажем. Тень бабблов — новая переменная `--shadow-bubble` в base.css (аппрокс., сверить с `Эффекты.png`). ✅ **Контент всех 5 табов получен и внедрён** (из `фото тз/блок 2 табы 2-5.png`): «Аттестат гос. образца», «Мини классы», «ДЗ без участия родителей», «Не нужен репетитор», «Без стресса и буллинга». У табов 2–5 добавлен подзаголовок (`.attestat__subtitle`), основной текст серый с тёмными акцентами, свои бабблы и своё фото. Данные — массив в начале `attestat.js`, клик по пиллу меняет заголовок/подзаголовок/текст/фото/бабблы. Фото табов 2–5 вырезаны из макета (577×377, `*-tmp.webp`) — **ждём чистые экспорты**. Коллаж аттестата отличается от обычных фото шириной, поэтому у него отдельный модификатор `.attestat__img--wide`. ✅ Мобилка по макету: белая карточка **во всю ширину экрана** без скруглений, пиллы **столбиком** 320×45 с шагом 55 (раньше был горизонтальный скролл — неверно), внутренний отступ 20.
5. **Выберите класс** — `classes.css`. ✅ Свёрстан по скрину клиента: синяя секция (`--gradient-accent`) с декором-вихрем `classes/classes-bg.webp` (из PNG 5.4МБ → 42КБ) в правом-верхнем углу; заголовок; сетка 8 карточек (4×2 на десктопе, 2 на планшете ≤1024, 1 на ≤560); в тексте акцент (тёмный) + `--muted` (серый); аутлайн-кнопки `хочу поступить` (`.btn--outline .btn--small`), у розовой «Экстернат» — белая кнопка. Все CTA → форма (`data-open-form`). Зазор до аттестата — 20px (нижний padding `.attestat` уменьшен до 20px). ❗Открыто: ховеры кнопок (сейчас стандартные из layout — заливка розовым); реальные тексты карточек — по скрину, сверить; декор-вихрь можно приглушить, если клиент захочет мягче.
6. **О нас** — `about.css`. ✅ **Пересобран по макету 19.07.2026** (Figma `1:3599` + сверка с 1920.png, блок y 2747–3775). Было расхождение почти по всем размерам, стало: сетка **две колонки 1fr/1fr, зазор 20** (930 + 20 + 929 = 1880 ✓; было `1fr 1.05fr` с зазором 32); в правой колонке **зазор 16**, а не 20 (`about__col--right`); padding карточек **60**, у лицензии — **50** (было 40 везде); высота карточки педсостава **603**; заголовки карточек 28/1.2, текст 18/1.4 (был 1.55). Карта: пропорция **929×677**, скругление 30, тень. Фото руководителя: круглая подложка Ø**359** цвета `--color-surface` (было Ø308 цвета `--color-gray-3`) + вырезка 308×266 поверх. Педагоги: круг Ø**180** (был овал 192×182), колонка 200, шаг элементов 20, имя 18/1.2, бейджи 45px с шагом 10. ❗**Цвета бейджей были неверные** (замер по 1920.png): стаж — фон `#CCE8FF` (`--color-blue-chip-bg`) с текстом `#2F54D8`, был `--color-light-blue` + `--color-blue`; предмет — фон `#2F54D8` (`--color-blue-chip`), был `--color-blue`. ❗Акцент в тексте лицензии в макете `#525252` (`--color-gray-1`), а стоял чёрный. Тексты всех четырёх карточек сверены с Figma **посимвольно** — совпали; убраны принудительные `<br>` в пояснении педсостава (в макете перенос естественный по ширине 474). **Закрыто экспортами из Figma:** hi-res скан лицензии (нода `1:3660` → 1450×1872, вместо прежнего апскейла из 156×200) и чистая вырезка руководителя с прозрачностью (нода `1:3606`) — CSS-поворот скана и `mix-blend-mode` для руководителя больше не нужны. Карту из Figma **не брал** — она ниже разрешением (1044×749) и с теми же впечатанными подписями, наша `map.webp` 1800×1327 лучше; чипы-метки остаются поверх подписей, их стиль приведён к Figma `1:3661` (padding 10/20, gap 10, иконка 24, текст 18, радиус 30, backdrop-blur). ✅ **Педагогический состав наполнен реально (28.07.2026):** карточка показывает топ-4 по стажу — Рябошапка (36), Мамаева (33), Абдулмеджидова (30), Самойлова (22). Фото — отдельные портретные кропы `assets/img/about/<slug>.webp` (headshot с лицом по центру, вырезаны из оригиналов презентаций по верху головы через rembg; в блоке 12 у тех же людей другой кадр «голова+плечи»). Хак `mix-blend-mode: darken` **убран** (он был нужен для присланных JPEG на белом фоне); бейджи прижаты к низу через `margin-top: auto`, чтобы при длинном ФИО в 3 строки (Самойлова) бейджи оставались на одной линии с остальными. ❗Заголовок секции в Figma стоит от x=50, но у «Тарифов» — от 40, у «Факультативов» — от 20: несогласованность макета, держим общий отступ контейнера ради ровного левого края — **на согласование**. **Ранее подключённые материалы клиента:** две колонки — слева карточки «Руководящий состав» (текст + круглое фото) и «Педагогический состав» (4 круглых фото + бейджи стаж(голубой)/предмет(синий)), справа карта филиалов и карточка «Государственная лицензия» (тёмный акцент в тексте + скан). Колонки по верху (`align-items:start`). **Фото педагогов** (заглушки `Panova/Kucherenko/Semenova/Pospelova.webp` из ранних набросков) удалены 28.07.2026 — карточка переведена на реальный топ-4 по стажу из `assets/img/staff/` (см. выше). **Карта** — решение клиента: статичный экспорт Яндекс.Карт `yamap.png` (метки «Гостиный двор»/«Арбат»/«Бутово» и рамка встроены в картинку) → `map.webp` (1800px, с прозрачностью, 3.3 МБ→287 КБ). Поверх карты — **3 HTML-чипа-метки** (`.map-pin`: белая пилюля + иконка `map-pin.svg` как в hero + текст). По решению клиента чипы **посажены точно поверх впечатанных в `map.webp` подписей филиалов, чтобы их закрыть** (координаты в % от карты: Гостиный 51/10.5, Арбат 45/15.5, Бутово 40/92). Размеры чипов — в `cqw` (`.about-map { container-type: inline-size }`), поэтому масштабируются вместе с картой и накрывают метки на любой ширине без коллизий. ❗Координаты привязаны к текущей `map.webp`; при замене карты — перепроверить позиции чипов. Карта подтвердила написание — «Гостиный двор». **Фото руководителя** — `direktor.webp` (вырезка из Figma с прозрачностью, 500×431, 21 КБ). Скан лицензии — крупная наклонённая «фотография» 348×450 (`assets/img/about/license.webp`); нижняя половина уходит за нижний край карточки и обрезается (`overflow:hidden`), без тени/фона по ТЗ. На ≤1100 скан встаёт под текст целиком (сбоку в узкую карточку не помещается). ❗Убраны кружки-маркеры (мини-фото + розовый «A») на фото руководителя и на карте — артефакты Tilda из макета, не дизайн. Адаптив: ≤768 одна колонка + педагоги 2×2, ≤375 руководитель в столбик (мобильного макета нет — приблизительно; проверка <500px недоступна в headless, сверить при получении макета). ⚠️ **Правки после проверки в браузере:** (1) у `.manager__photo` и `.license__img` не была задана `height` — высоту брал HTML-атрибут (431 и 900), картинки рисовались вдвое крупнее: руководитель уезжала под срез карточки (видна была одна макушка), скан лицензии наезжал на текст. Добавлен `height: auto` — **правило для всех абсолютных картинок с `width` в процентах**. (2) Ширина текста в обеих карточках была в фиксированных px (432 и 521), а картинки справа спозиционированы в процентах, поэтому на промежуточных ширинах 1601–1919 текст наезжал на круг — переведено в проценты (53.3% и 62.8%). (3) Фото руководителя выглядело «обрезанным»: размеры круга и фото взяты из Figma и считаются от **ширины** карточки, а её высоту задаёт текст и на ширинах кроме 1920 она растёт — между низом фото и низом карточки открывалась полоса пустого круга с прямой границей вырезки. Размеры оставлены строго по Figma (круг 359 при x=531, фото 308×266 при x=557), но **обе фигуры привязаны низом, а не верхом**: фото — `bottom: 0` (срез вырезки совпадает с границей карточки и не виден), круг — `bottom: 0; margin-bottom: -7.3%` (в макете круг 359 при карточке 311, то есть свисает на 68px ниже края и подрезается; 68/930 = 7.3%, а процентные `margin` считаются от **ширины** контейнера, поэтому свес масштабируется вместе с кругом). Если держать круг за верх, он на широких карточках «всплывает» и целиком помещается внутрь — подрезки не видно, вид не по макету. Замер на 1920: круг 359 при left 531, свес 68 — совпадает с макетом; top выходит 8 против 20 в Figma, потому что наша карточка 300 против 311 в макете (текст переносится чуть иначе). (4) Колонки стояли `align-items: start` и на нестандартных ширинах расходились по высоте (пустое место внизу левой); теперь `stretch`, а разницу добирает последняя карточка колонки (`.teachers` / `.license` с `flex: 1`), в макете низ колонок совпадает. Проверено на 1920 / 1700 / 1280: наездов нет, колонки равны, горизонтального скролла нет. ⚠️ **Правка 21.07.2026 (просьба клиента): круг и фото директора уменьшены** — круг с 38.6% до **33%** ширины карточки, фото с 33.1% до **28.5%**, центр обеих фигур сохранён на 76.4%. Причина: круг и макушка обрезались верхом карточки. Корень расхождения с Figma — высота карточки: в макете она 311, а рендерится 240, потому что текст руководителя занимает меньше строк, чем нарисовано. Ограничение считается по самой НИЗКОЙ карточке, а это ширина 1920 (текст идёт в 496px и переносится реже всего); на 1600 и 1280 карточка выше и запас только растёт (замеры просвета над кругом: 12 / 19 / 53). Фото по-прежнему привязано низом — срез вырезки совпадает с краем карточки. Если клиент захочет вернуть макетные размеры, увеличивать надо не фигуры, а высоту карточки (`.manager { min-height: ~16.5cqw }` = 311 на 1920).

**Жду:** согласовать состав и стаж педсостава (топ-4 по стажу выбран автоматически из презентаций; при желании клиента — включить директора Плесецкую, 28 лет, вместо Самойловой). Все остальные материалы блока получены.
7. **Вузы** — `universities.css`. ✅ Пересобран точно по Figma `1:221` (баннер 1880×393, Y 3795–4188 в `1920.png`; padding 60, radius 30, `--gradient-accent` + `--shadow-card`).
   - **Заголовок** (`1:885`, 48px / lh 0.9 / ширина 707, 3 строки): края приглушены до `--color-light-blue` («Наши выпускники», «страны и мира»), середина под маркером, выступ по бокам 3px. Цвет заливки — **общий с H1 в hero** (`color-mix(--color-light-blue 40%, transparent)`), по решению клиента вместо белого 20% из Figma.
     - ⚠️ Заливка задана `background-image` + `background-size: 100% 0.9em`, а не обычным `background`. Причина: у инлайна фон заливает content area шрифта (≈1.15em ≈ 55px), а строки идут с шагом `line-height` 0.9em (43px) — плашки соседних строк наезжали друг на друга светлой полосой (клиент это заметил). С явной высотой в `0.9em` они встают встык, как в макете, и высота едет вместе с `font-size` на брейкпоинтах. Скругления нет — в Figma у плашек оно 0.
     - ⚠️ В Figma у плашек стоит `blur(20)`, но `backdrop-filter` на многострочном инлайне браузер считает по общему боксу всех строк — блюр съедал соседние «Наши выпускники» (текст исчезал). Блюр убран (плашка от x=56.6 при тексте от x=60). Шаг строк сверен по пикселям `1920.png` — ровно **43px**, т.е. lh 0.9; плашки в макете нарисованы выше строки (49) и слегка наезжают — намеренно не копируем, заливка по высоте строки.
   - **Логотипы** (группа `1:890`, 804.21×273) — 12 белых SVG, **получены** (`univer-*.svg`, уже `fill="white"`). Вся сетка выводится из одной переменной `--u` = 113.75: колонки `repeat(6, u)`, шаг по X `0.214u` (24.34), по Y `0.3u` (34.13), высота `2.4u` (273) → бокс 7.07u × 2.4u совпадает с макетом до десятых. Ряд 1 прижат к верху ячейки (`align-self: start`), ряд 2 центрирован (все центры на 204.75). «Оксфорд» единственный выше ячейки — `1.2u` (136.5), симметрично выступает вниз.
   - **Фон получен из Figma**: `bg-pattern.svg` (сетка, `1:222`) и `bg-blend.svg` (волна-«лента», `1:279` — 601 круг r=82 вдоль кривой). Позиции в `cqw` от ширины баннера, поэтому масштабируются с ним; лишнее срезает `overflow: hidden` + скругление. Ждать от клиента больше нечего.
     - **Сетка рисуется в CSS, а не картинкой** (`.universities__banner::before`, два `repeating-linear-gradient`, шаг 52.4 / линия 1px / `rgba(255,255,255,.15)`). Экспорт `1:222` пришлось отбросить: он покрывает только правую часть баннера (1276×390 от x=603) и вдобавок гаснет изнутри — у всех 30 линий градиентные обводки с `stop-opacity="0"`, поэтому «до упора вправо и вниз» из него не выжать ни растяжением, ни правкой прозрачности (пробовали оба). CSS-сетка тянется на весь баннер (`inset: 0`) и упирается во все четыре края; слева гасится `mask-image` (35%), иначе лезет на заголовок. Файл `bg-pattern.svg` удалён за ненадобностью.
     - ⚠️ Общий урок по экспортам Figma: прозрачность часто зашита **внутрь** SVG (`<g opacity>`, `stop-opacity`) — дублировать её в CSS нельзя (сетка была невидима из-за 0.15 × 0.15).
     - ⚠️ **Волна растянута против координат Figma**: по номиналу (−160/−140, 2084×704) её правый скруглённый торец обрывается внутри баннера на x≈1841 — виден срез. Поставлено `left: −10cqw; top: −11cqw; width: 135cqw`, тогда оба конца уходят за края, низина ложится на нижнюю кромку, а правый подъём приходится на МГУ — как в `1920.png`. Подобрано сравнением рендера с макетом.
     - Из экспорта волны удалён служебный `<path stroke="#2F2C2C">` — направляющая кривая, к дизайну не относится.
   - **Адаптив**: ≤1600 текстовая колонка сужается до 471 (иначе выдавливает сетку из баннера 1580); ≤1279 — столбиком, padding 40; ≤768 — 4 колонки, `--u` 88; ≤560 — секция **типа A** по `Tz/mobile-360.md` (во всю ширину, без скруглений и бокового поля, padding 50/20, gap 30), 3 колонки по 79 с зазором 2, кнопка на всю ширину с радиусом 10, паттерн скрыт (в мобильный фрейм не попадает), волна обрезается левым краем — как в `1:9775`.
   - Право использования логотипов вузов — на клиенте.
8. **Тарифы** — `pricing.css`. 4 карточки (бейджи, список с галками, цена с подсветкой, CTA). Свёрстан с заглушками макета. ⚠️ **19.07.2026 блок закомментирован в `index.html`** (секция `<section class="pricing">` + якорные ссылки «Тарифы» в шапке, мобильном меню и футере) — на странице не показывается, пока клиент не пришлёт реальные тарифы. `pricing.css` и его `<link>` оставлены на месте: чтобы вернуть блок, достаточно снять 4 комментария. Жду: реальные названия/описания/пункты/цены; есть ли «рекомендуем».
9. **Баннер 70%/ЕГЭ** — `ege-banner.css`. ⚠️ **Перенесён по решению клиента (19.07.2026):** в макете блок стоит здесь, после «Тарифов», а на странице теперь идёт **сразу за «Выберите класс»** — в `index.html` это секция №6, порядок `<link>` на CSS переставлен следом. Побочный эффект переноса устранён: у `.classes` нижний padding был 80px как разгон под заголовок «О нас», который шёл следом; теперь `.classes` снизу 20, а разгон держит сама `.about` (`padding-top: 100` вместо 40) — суммарный отступ до заголовка прежний, 120px, и он больше не зависит от того, какой блок стоит перед ним. ✅ Свёрстан точно по Figma `1:3836` (карточка 1880×292, Y 5052–5344 в `1920.png`; padding 60, radius 30).
   - **Фон сплошной `--color-pink` (#FF2A7F)**, а не градиент: переливы на макете даёт декор-вихрь `ege/wave.webp` (`1:3837`, 1899.23×676.15 в точке 262/−12 — втрое выше баннера, лишнее срезает `overflow`). Заведённая ранее переменная `--gradient-pink` была ошибкой чтения макета и **удалена** из `base.css`.
   - **Заголовок** (`1:4441`, 48px / lh 0.9 / ls −0.02em, белый, ширина бокса 941) — **четыре строки с ручными переносами**: строки 551/495/484/472px при боксе 941, т.е. естественного переноса там нет, в Figma они проставлены руками. Маркер в макете нарисован двумя плашками (280×49 на 2-й строке и 497×49 на 3-й); в вёрстке это один инлайн-span с `box-decoration-break: clone` — даёт те же две плашки, выступ по бокам 3px. **Два намеренных отличия от Figma, оба — как в `universities.css`:** (1) `backdrop-filter: blur(20)` не применяем — у многострочного инлайна браузер берёт общий бокс всех строк, и блюр съедает соседний текст (в макете плашки лежат ПОД текстом, а фон инлайна красится ПОВЕРХ уже отрисованных строк); (2) без вертикального padding — в макете плашка выше строки (49 против 43.2) и наезжает на соседнюю.
   - **Никакого абзаца под заголовком в макете нет** — прежний текст-заглушка («Подготовка по проверенным программам…») был выдуман и удалён.
   - **Экспорты получены, ждать от клиента нечего:** вырезка ученицы `girl.webp` (`1:7094`, 387×292 в точке 866/0 — во всю высоту баннера) и её мобильный вариант `girl-mobile.webp` (`1:11793`, 340×314, **другая обрезка исходного фото**, подменяется через `<picture>` на ≤1024); бейджи `fgos.webp` (`1:4443`) и `fipi.webp` (`1:4444`). Всё в WebP, суммарно ~254 КБ, `loading="lazy"`.
   - **Бейдж ФГОС повёрнут на −14°**: Figma отдаёт его габаритом 174.56×174.13 (это AABB повёрнутого квадрата), сторона 143 и угол посчитаны по `1920.png` подгонкой минимального прямоугольника. Радиус 20 — в локальных координатах карточки.
   - Декор спозиционирован в `cqw` — масштабируется на 1600/1200. ⚠️ **`container-type` висит на `.container`, а не на самой карточке:** `cqw` считается от **content-box** контейнера, и на карточке с `padding: 60` единица вышла бы 1760/100 вместо 1880/100 — весь декор уезжал влево и мельчал на 6%. У `.container` content-box (ширина минус `--page-gap`) в точности равен ширине карточки на всех брейкпоинтах. **В `universities.css` эта ошибка есть** (`container-type` на `.universities__banner` с `padding: 60`) — фоновые слои там смещены на те же 6%; поправить при ревизии блока.
   - Сверено с `1920.png` попиксельно: все 4 строки заголовка (81..631 / 83..577 / 83..566 / 84..555) и оба бейджа совпадают с макетом с точностью 1–2px.
   - **Адаптив**: ≤1024 — раскладка мобильного фрейма `1:11187` (заголовок сверху, вырезка 340×314 под ним по центру, бейджи переезжают в координаты относительно вырезки); боковая композиция ломается раньше, чем кажется — уже на ~800px строки заголовка догоняют вырезку. ≤560 — секция **типа A** (во всю ширину, без скруглений и бокового поля, padding 50/20).
10. **Образовательные программы** — `programs.css`, `programs.js`. ✅ **Переписан целиком 19.07.2026** (был заглушкой-«фото» с придуманными градиентами). Данные всех трёх расписаний получены из Figma (секция «Расписание» `1:20850`) и записаны в `Tz/raspisanie.md`; сверены со скриншотами клиента `Tz/1921.png`, `Tz/1922.png`. Раскладка: две карточки в ряд `1167fr / 693fr`, gap 20 (20+1167+20+693+20 = 1920 ✓), обе тянутся на одну высоту. Белая карточка (padding 60, gap 40): h2 62px с принудительным переносом → 3 таба равной ширины (335, высота 52) → подзаголовок 28px → сетка → распорядок. Сетка 5 колонок по 167 с gap 10, **прижата влево** (`max-width: 875`) — справа остаётся белое поле, так в макете и на скриншоте клиента. Колонки независимы (строки между днями не выровнены), но сумма четырёх содержательных плашек в каждой = 230, поэтому пустые слоты внизу всё равно на одной линии. Плашка: radius 10, 45px в строку / 70 в две. ❗Боковой padding плашки **20, а не 30 как пишет Figma**: там текст в режиме hug вылезает за собственный padding; 167−2×20 = 127 — единственное окно, воспроизводящее переносы макета («Русский язык» в строку, «Английский язык» в две). Палитра — 10 пар токенов `--chip-*-bg/-fg` в `base.css`; ❗цвет **не привязан к предмету** (та же «География» в средней школе фиолетовая, в старшей розовая), поэтому оттенок задаётся в данных рядом с названием. Синяя карточка (Figma `1:4536`): заголовок 62px, «Приходите к нам на» — `--color-light-blue`, остальное белым; два баббла-стикера с наклоном (+4.6° светлый, −6° тёмный, замер по макету); мальчик — **чистый экспорт из Figma** (нода `1:4594`, вырезка с прозрачностью → `programs/boy.webp`, 70 КБ), раньше стояла заглушка из hero; сетка-декор переиспользована из hero (`hero-line.svg`, opacity 0.15). ✅ **Высота карточки одинакова для всех трёх табов** (просьба клиента 19.07.2026): раньше без распорядка дня белая карточка проседала с **706 до 619** и вместе с ней прыгала соседняя синяя. `syncCardHeight()` в `programs.js` прогоняет все три программы, берёт самую высокую и ставит её как `min-height`; пересчёт на `document.fonts.ready` (до подгрузки Onest строки переносятся иначе) и на `resize` с задержкой 150мс. Считается замером, а не константой в CSS, — когда придут распорядки для средней и старшей, выравнивание останется верным. **Жду:** ❗**распорядок дня средней и старшей школы** — в макете он есть только у начальной, блок распорядка скрывается сам, если текста нет. ❗Расхождения макета на согласование (подробно в `Tz/raspisanie.md`): пустые слоты в середине колонок у средней/старшей (в вёрстке уроки идут подряд сверху, пустые вниз — как у начальной); неунифицированные названия («Английский язык» vs «Английский», «Литературное чтение» vs «Литература»). Мобильная раскладка сетки — приблизительная (5 тесных колонок), уточнить по `Tz/mobile-360.md` (ноды `1:14359` и `1:11797`).
11. **Нет домашних заданий** — `no-homework.css`. ✅ **Перевёрстан по Figma 19.07.2026** (был на временной вырезке из `1920.png` с впечатанной кнопкой). Слои в макете лежат раздельно, поэтому вырезка-костыль и оверлей-кнопка убраны: декор-вихрь `1:4606` → `no-homework/wave.webp` (86 КБ), вырезка мальчика `1:5310` → `boy.webp`, мобильный кроп того же исходника `1:12571` → `boy-mobile.webp`, старые `boy-tmp.webp` / `boy-narrow-tmp.webp` удалены. Карточка (`1:4605`) 1880×222 на `--color-light-blue`, padding 60, radius 30, раскладка `row / space-between / align-end`: слева текст 1081 (`--fs-h4`, `--color-blue`, розовый акцент), справа CTA. Вихрь и мальчик — абсолютные слои в `cqw` от ширины карточки (`container-type` на `.container`, не на карточке), лишнее срезает `overflow`; порядок слоёв как в Figma: вихрь → текст → мальчик → кнопка. ⚠️ **Ловушка вихря:** Figma отдаёт ноду как 1224.34×687.72, а рендер приходит 1207×608 (группа отмасштабирована неравномерно) — при `height: auto` лента уезжает. Замером по `1920.png` (границы ленты на строках 6310/6360/6480/6528) подтверждён вариант с **растяжением** в 1224.34×687.72, `height` задан явно. Положение мальчика тоже сверено с макетом — совпадает в пределах 1–3px. Кнопка: в Figma стрелка лежит в боксе 42×42 (итог 261×86), здесь развёрнута в плоские отступы `22px 33px 22px 40px` + `gap: 21` + `min-height: 86` — ширина и высота сходятся с макетом 1-в-1. **Адаптив:** ≤1200 — текст без `max-width`; ≤1024 — мобильный фрейм `1:11866`, секция **типа A** (во всю ширину, без скруглений, padding 50/20), порядок текст → CTA (320×48, radius 10) → вырезка во всю ширину; вихрь там привязан к **низу** карточки (`bottom: -82.144cqw`), а не к верху, иначе разъезжается с вырезкой при переливании текста на другое число строк. ❗В макете «весь материал **отрабатываются и закрепляют**» — согласование сломано, в вёрстке «отрабатывается и закрепляется», **на согласовании**. ❗Исходник мальчика в Figma мелкий (кроп 424×220 при показе 495×256) — **запросить у клиента фото в большем разрешении**.
12. **Обучайтесь комфортно** — `staff.css`, `staff.js`. ✅ **Перевёрстан пиксель-в-пиксель 19.07.2026** по абсолютным координатам Figma (ноды `1:7082` заголовок, `1:5336` табы, `1:5316` карточка, `1:5334`/`1:5335` скроллбар, `1:5332` фото, `1:5347` бейджи+текст, `1:5357` стрелки) — все замеры выписаны в шапку `staff.css`. Сетка карточки (content-box 1760 = 1880 − 2×60): `107.22 | 70.85 | 472.04 | 49.89 | 360 | 69 | 545.91`, остаток **85.09 справа** (правая колонка не доходит до внутреннего края — так в макете). Строки `350 + 20 + 50` (было 19). Имя, фото и правая колонка центрированы по высоте карточки — сверено по Y (6989 / 6887 / 6975 при карточке 6797…7337). ❗**Табы разошлись между источниками:** в `1920.png` — 6 ролевых (учителя/профориентологи/психологи/наставники/кураторы/тьюторы), в Figma — **5 предметных** (руководящий состав / начальная школа / иностранные языки / история и обществознание / география, биология, химия). По решению 19.07.2026 актуальна Figma, PNG-экспорт по этому блоку устарел. Пилл: `padding 14/30`, `20/1.2`, radius 89, `flex: 1` (по 360 на 1880) + `min-width: min-content`, чтобы длинный «география, биология, химия» не резался на 1600/1400; на ≤1279 — горизонтальный скролл. ❗Миниатюры: неактивная на `--color-surface`, активная на `--color-blue` — **затемнения/opacity в макете нет** (раньше стояли opacity .55 и синее кольцо). Бейдж-солид: фон `--color-blue-chip`, текст **`--color-blue-chip-bg` (#CCE8FF), не белый**. Акцент в текстах — `--color-gray-1` (#525252) и в лиде, и в описании. Стрелки: шеврон **7.66×13.29**, обводка 1px `--color-light-blue`, цвет `--color-blue-2`. **✅ Наполнено реальными данными 27.07.2026** из презентации клиента «ПЕДАГОГИ БУТОВО.pptx»: 10 педагогов в 5 предметных категориях (руководящий состав 3 / начальная школа 1 / гуманитарные науки 3 / точные и естественные науки 2 / иностранные языки 1). Категории заменены с макетных Figma на предметные по согласованию с клиентом. По просьбе клиента **исключены 7 человек** (Бачурина, Лебедева, Панова, Кудрина, Крылова, Семикян, Савицкая) — среди них была и Панова, единственная с реальным текстом в старой версии. Фото: главное круглое — кроп «голова+плечи» из снимков презентации (PIL, 400×400 webp, `assets/img/staff/<slug>.webp`); миниатюра — вырезка без фона (`rembg`/u2net, `<slug>-thumb.webp`). Старые фото (Панова/Кучеренко/Поспелова) из блока не ушли из `about/` — они ещё используются педсоставом блока 7 «О нас». **На согласовании:** сжатые описания (`lead`/`text` скомпонованы из образования/стажа/достижений презентации) и бейджи без стажа у двух администраторов (Ялыманова — «РЭУ им. Плеханова», Данилова — «мама пятерых детей»). ⚠️ Исходники фото — из PowerPoint, разного качества (Мамаева кроп всего 331px → лёгкий апскейл): если у клиента есть hi-res портреты, лучше заменить. ❗**На согласование:** заголовок блока в Figma стоит от `x=40`, тогда как карточка и табы — от `x=20`; в вёрстке оставлен общий контейнер (20), потому что на странице заголовки секций нарисованы то от 20, то от 40, то от 50. ❗Адаптив ниже 1920 — на глаз: адаптивные фреймы Figma (`1:22821` 1600, `1:21396` 1200) обрываются задолго до этого блока.
13. **Приходите познакомиться** — `invite.css`. ✅ **Перевёрстан пиксель-в-пиксель 19.07.2026** по Figma `1:5362` (был на временной вырезке `kids-tmp.webp` с маской). Баннер **1880×320** (а не 340 — 340 это шаг до следующей секции, включая зазор 20), padding 60, radius 30, контент по вертикали по центру, колонка с gap 40. Заголовок 48/0.9/**−0.02em**, первая строка белая под плашкой, вторая — `--color-light-blue` (раньше обе были белые). Замеры сняты прямо с `1920.png` и сошлись с Figma: плашка 1054×49 при сдвиге −3/−1, кнопка 340×74, дети 602×308 вровень с правым и нижним краем. Слои декора разделены на экспорты: сетка `1:5363` → `invite/grid.svg` (opacity .15 вшита), вихрь `1:5420` → `wave.svg`, дети `1:5929` → `kids.webp` (67 КБ), `kids-tmp.webp` удалён. Всё позиционирование — в `cqw` от `.container`. ⚠️ **Ловушка вихря:** Figma отдаёт рамку ноды 725.99×595.93, а экспорт обрезан по краске (585.75×543) — краска поставлена по центру рамки (867.7 / −134.9), совпадение с макетом проверено расчётом левой кромки вихря по строкам `1920.png`. Плашка-маркер: высота задана **фоном** `background-size: 100% 49px`, а не padding (у разорванного на строки инлайна padding наехал бы на соседнюю строку); `backdrop-filter` из Figma не переносится. Кнопка: у общей `.btn` gap 12 и стрелка 20×20, в макете gap 10 и рамка иконки 30×30 — уточнено локально в `.invite__btn`, даёт ровно 340×74. **Адаптив:** ≤1600/≤1200 — кегль из `base.css`, плашка 33/30, кнопка компактная (padding 14/30, стрелка 20); ≤560 — мобильный фрейм `1:12621`, секция **типа A** (во всю ширину, без скруглений, padding 50/20/0), сетка на своих координатах (−188; 12), вихрь скрыт (в мобильном фрейме он целиком за правым краем), CTA во всю ширину с radius 10, дети 461×236 выступают за левый край. ❗**На согласование:** в мобильном макете кнопка подписана «хочу на пробн**ые дни**!», на десктопе — «хочу на пробн**ый день**!»; оставлен десктопный вариант. ❗В `base.css` для h3 стоит letter-spacing 0 (по `Типографика.png`), здесь взято −0.02em из Figma — иначе первая строка не влезает в замеренные 1048px и плашка расходится с макетом.
14. **Отзывы** — `reviews.css`, `reviews.js`. ✅ **Возвращён на страницу 31.07.2026** (27.07 был вынесен в `disabled-sections.html`): секция стоит между «Приходите познакомиться» и FAQ, ссылки «Отзывы» вернулись в шапку, бургер-меню и футер. Роликов по-прежнему нет — плитки пустые, клик по ним ничего не открывает (страж `if (!item.src) return;` в `reviews.js`), заполняется массив `items` в начале файла. ℹ️ Возврат сделал в шапке 7 якорей и меню перестало помещаться (на 1281 перелив 17px) — по решению клиента пункты свёрнуты в **три выпадающих списка** («О школе» / «Обучение» / «Полезное»), «Отзывы» в третьем; `.header__nav` ужалась с 726 до 298px, перелива нет. Названия групп придуманы нами — ждём, подойдут ли клиенту. Детали в `CLAUDE.md`, блок 1. ✅ Свёрстан: розовая карточка 1880×892, сетка 4×2 плиток 425×300, заливки белым с прозрачностью (плитка 15%, круг play 10%, треугольник 62% — посчитано по альфе). ❗В самом макете плитки тоже пустые, без постеров — это подтверждает, что формат не выбран. В `reviews.js` у карточки есть `type: 'mp4' | 'embed'`: для mp4 вставляется `<video>`, для эмбеда `<iframe>`, открытие — через общий лайтбокс из `main.js`. **Жду:** решение по формату + сами ролики и постеры. Пока `src` пустые, клик ничего не открывает.
15. **FAQ** — `faq.css`, `faq.js`. ✅ Свёрстан целиком и **выверен по пикселям** (Figma `1:5930` + сверка с `1920.png`), **контент получен**: все 9 вопросов и ответов из `фото тз/FAQ ответы.png`. Белая карточка 1880×1192 (Y 8609–9800), padding 60; колонки `617 + 38 + 1105` заданы как `flex: 0 1 <базис>` — на 1920 сумма сходится точно, ниже колонки ужимаются пропорционально. Заголовок разбит на 5 строк **ручными `<br>`** (по ширине 617 текст так бы не переносился); те же переносы верны и на мобилке. Айтемы на `--color-surface` (radius 20, padding 30): закрытый — ровно **100** (высоту держит `min-height: 40` строки вопроса = размер шеврона), открытый — **164** (28×1.2 + 20 + две строки 18×1.4). Ради этих 164 шеврон **вынесен из потока** (`position: absolute` от айтема, клик ловит кнопка-родитель) — в потоке его 40px растянули бы открытый пункт до 170. У вопроса «Есть ли скрытые доплаты…» в данных стоит `w: 701` — в макете его текстовый блок ужат, иначе перенос уезжает с «материалы» на «или». Шеврон 40×40: открыт — заливка `--color-blue`, закрыт — обводка `--color-light-blue`; иконка — точный экспорт Figma `1:6938` (13.29×7.66, смотрит вверх, закрытому разворачивается CSS). Раскрытие через `grid-template-rows: 0fr→1fr`. **Декор получен:** `faq/swirl.webp` — экспорт Figma `1:5931` (2175×994, 158 КБ). ⚠️ Габариты из Figma (2428×2106, поворот −150°) **обманывают**: экспорт отдаётся уже в финальной ориентации и обрезанным по краске, поэтому позиция подобрана численно совмещением с `1920.png` — **без поворота, масштаб 1:1, смещение −746 / +514** от угла карточки (в `cqw`). Обязателен `max-width: none` — иначе глобальный `img { max-width: 100% }` схлопнет 2175px до ширины карточки.
16. **Факультативы** — `electives.css`. ✅ Свёрстан: бенто 3+2+3, высоты 287/289/287, во 2-м ряду «Каллиграфия» — фикс 909px. 8 карточек, **описания заполнены у всех восьми** (тексты клиента 22.07.2026; у «Изучаем языки» и «Творчество» прежние перечисления из макета заменены — весь блок теперь набран одинаково, живым предложением с точкой). Тогда же **сняты ручные переносы `<br>` в заголовках** «Центр подготовки к ЕГЭ и ОГЭ» и «Бесплатные занятия» — заголовки резиновые, перенос делает браузер, предлоги держат неразрывные пробелы. Замер после правки (WebKit, 1920/1600/1280/1024/768/430/360): переполнений нет, высоты бенто остались макетными 287/289/287, горизонтального скролла нет; на 360 самый плотный случай («Центр подготовки»: заголовок 2 строки + описание 3) оставляет 28px до кнопки. Кнопки открывают форму с предзаполненной темой (`data-open-form="Название"`). ❗В макете «Центр подготовки **к егэ и огэ**» строчными — в вёрстке «к ЕГЭ и ОГЭ», **на согласовании**. **Жду:** подтверждение, что «узнать подробнее» ведёт именно в форму, а не на отдельные страницы.
17. **Футер** — `footer.css`. Белое лого ✅, якоря ✅ (❗без «Кейсы» и «Преимущества»), плашка лицензии ✅ (`assets/img/gos_liz.png`), нижний ряд ✅ (копирайт, политика/согласие-заглушки, реквизиты ИП, дисклеймер). Меню 2 колонки ✅ (❗в макете есть «Кейсы» — не добавлен, секция удалена). Реквизиты по полному скрину: ИНН 772772917590, ОГРНИП 312774616600011. Чипсы Яндекс 5.0 / 2ГИС 5.0 ✅ (`yandex.svg`, `2gis.svg`, `star.svg`; href-заглушки). Жду: ссылки на профили Яндекс/2ГИС, тексты политики и согласия (формат: отдельные HTML или PDF), сверку реквизитов.
18. **Форма** — `form.css`, `form.js`. ✅ Свёрстана по `фото тз/Форма.png`. ❗**Важно: формы-секции на странице нет** — в макете (и в Figma, фрейм `1:7139`) она существует только как поп-ап поверх затемнённого фона. Пустая `<section class="form" id="form">` из `index.html` удалена, модалка лежит в конце `<body>`. Окно 1405px на `--gradient-accent`, оверлей `rgba(0,0,0,.6)` + `blur(5px)`, заголовок 48px по центру, крестик 50×50. Поля: имя, телефон, радио-выбор школы (Гостиный двор / Арбат / Бутово), чекбокс согласия (включён по умолчанию). Поля — без рамки, только нижняя линия, как в макете. Маска `+7 (___) ___-__-__`, валидация имени/телефона/согласия с подписями ошибок, блокировка скролла, закрытие по Esc и клику вне, удержание фокуса внутри модалки, возврат фокуса при закрытии. Экран успеха показывается вместо формы в том же окне. Открывается `window.openForm(topic)` — все `[data-open-form]` на странице ведут сюда, тема уходит в скрытое поле. ❗Опечатка макета «Выберете школу» исправлена на «Выберите», **на согласовании**. ❗Декоративная светлая лента из макета заменена мягкими CSS-бликами: вырезать её из `Форма.png` нельзя — поверх напечатаны поля. ✅ **Решено 28.07.2026:** кастомная форма ОТКЛЮЧЕНА — все CTA открывают **CRM-форму Битрикс24** (`click/225`, портал `lancman.bitrix24.ru`) через `js/bitrix.js`; заявки уходят в CRM, кастомный `sendLead()` больше не используется. Модалка и логика остались в коде (для отката убрать `<script src="js/bitrix.js">`). Проверено end-to-end в браузере. Подробности — в `CLAUDE.md` (раздел «Аналитика/CRM-форма» и блок 18). ⚠️ Тема заявки (`data-open-form`) в Битрикс не передаётся.
19. **Адаптив** — внутри CSS каждого блока (медиазапросы), брейкпоинты уточню по мобильным макетам (ориентир: 375 / 768 / 1280 / 1920). Жду: мобильные фотки всех секций, можно партиями.

## Порядок работ

1. ✅ **Каркас:** index.html со скелетом секций (порядок = макет), base.css (переменные из `Цвета.png`/`Типографика.png`/`Эффекты.png`) + layout.css (контейнер, кнопки по `Состояние кнопок.png`), шапка + мобильное меню, футер-каркас на градиенте. ❗Фон страницы: клиент указал F8F8F8, в `Цвета.png` «фон» = D8E6FB (совпадает с «голубой») — согласовать. Onest — variable font, самохост `assets/fonts/` (cyrillic/latin/latin-ext сабсеты, OFL.txt), preload в index.html. ❗Боковой зазор 20px от краёв экрана — единый стандарт по правке клиента (было 80px). Для блоков без фона (шапка/футер) — через `.container { padding-inline: 20px }`. Для блоков с белой карточкой (напр. «Аттестат») зазор задаётся СНАРУЖИ карточки — через `padding` секции (`.attestat`), иначе фон карточки закрашивает внутренний padding и зазора не видно. Hero/инфо-карточки — свои отступы (12px / 32px).
2. **Шапка + hero + футер + форма** (форма — по фоткам состояний).
3. **Статичные секции** (3–9, 11, 13, 16) — временно с фонами, вырезанными из 1920.png; замена на экспорты по мере поступления.
4. **Интерактив:** programs.js, staff.js, reviews.js, faq.js.
5. **Адаптив** по мобильным фоткам.
6. **Контент и полировка:** реальные тарифы/расписания/педагоги/видео, WebP, lazy-load, мета/OG/фавикон, метрика (нужна? — уточнить), вычитка опечаток.

## Проверка

- Playwright: скриншоты 1920/1280/768/375, сверка с макетами из «фото тз».
- Прокликать: якоря, табы расписания (контент меняется), слайдер персонала (категории/миниатюры/стрелки), аккордеон, видео, маска телефона, форма → успех.
- Вес страницы: фоны WebP, изображения ниже первого экрана — lazy.

## Ревью вёрстки против макета (19.07.2026)

Метод: `1920.png` совпадает с макетом 1:1 по пикселям (и идентичен `Десктоп целыый.png`),
поэтому геометрия снята замерами, а не на глаз.

**Эталон:** все блоки — ширина **1880** при боковом отступе **20** (футер полноширинный 1920),
вертикальный зазор между карточками — **20**.

| # | Блок | Дефект | Статус |
|---|------|--------|--------|
| 10 | Программы | `max-width: 1280px` при макете 1880; заглушка-«фото» вместо сетки; хардкод 7 hex и 5 размеров | ✅ **исправлено** — блок переписан |
| — | Адаптив | боковое поле 20 держалось до 560; в макете с 1600 оно **10**, радиус карточек на 1200 — **20** | ✅ исправлено (`--page-gap`, `--radius-card`) |
| 9 | ЕГЭ-баннер | хардкод розового градиента | ✅ исправлено (`--gradient-pink`) |
| 3 | Инфо-карточки | боковой отступ и gap не по макету | ✅ исправлено (40 и 20, см. ниже) |
| 8 | Тарифы | хардкод `font-size: 38px / 32px` | ✅ исправлено (`--fs-h3`) |
| 7 | Вузы | 1.5 МБ SVG-логотипов (МПГУ 488 КБ, РАНХиГС 326 КБ) — избыточная точность координат | ✅ 917 КБ (−38%), округление до 1 знака |
| — | `<head>` | нет `description`, OG-тегов, фавикона | ✅ добавлены (фавикон — набор от клиента, 21.07.2026) |
| — | Репозиторий | ~22 МБ неиспользуемых исходников PNG | ✅ удалены, `assets/img` 24 МБ → 2.4 МБ |
| 1 | Шапка | высота **88px**, в макете (Figma `1:7109`, замер по 1920.png) — **61px** (16 + лого 29 + 16) | ⚠️ **не трогал** |
| — | Все секции | вертикальный зазор между карточками в макете **20**, по факту 40: соседние секции дают по 20 сверху и снизу | ⚠️ **сквозная правка, не делал** |

### ⚠️ Опровергнутые находки прошлого ревью — правки НЕ требуются

Две записи прошлого прогона оказались ошибочными; хорошо, что проверил до правок —
«починка» сломала бы корректную вёрстку.

1. **«Двойной боковой padding → карточка 1840»** (аттестат, классы, вузы, ЕГЭ-баннер) — **неверно**.
   `.container` и `.attestat__inner` — это **один и тот же элемент**
   (`class="container attestat__inner"`), а не вложенные. Специфичность обоих селекторов 0-1-0,
   но CSS блока подключён **после** `layout.css`, поэтому шорthand `padding: 48px` перекрывает
   `padding-inline` контейнера. Ширина карточки = 1920 − 2×20 = **1880 ✓**, внутренний отступ 48.
2. **«Логотипы вузов — цветные SVG без обеления»** — **неверно**. Все 12 файлов уже содержат
   только `fill="white"` и `fill="none"`, растра внутри нет. `filter` не нужен.

Отдельно уточнён замер инфо-карточек: в прошлом ревью стояло «отступ 42, gap 24»,
на деле по 1920.png (ряд от x=40, карточка 445, зазор 20) и по Figma `1:3569`
(x=40, ширина 1840, gap 20) — **40 и 20**. На 1600/1200 — 30 и 10.

**Проверено и признано корректным:** hero (панель 1880, наезд инфо-карточек ≈ `--overlap: 110`),
«О нас» и «Тарифы» (ширина верна), блоки 11–16 (`padding-block`, ширина верна),
футер (полноширинный).

## Сводный чек-лист вводных

**Ждём сейчас (самое блокирующее):**
1. ✅ **Куда отправлять заявки — РЕШЕНО 28.07.2026: CRM Битрикс24.** Кастомная форма отключена, все CTA открывают форму `click/225` (портал `lancman.bitrix24.ru`) через `js/bitrix.js`. (Ранее: 19.07 оставляли имитацию `sendLead()`.)
2. ✅ **Сотрудники блока 12 получены** (презентация «ПЕДАГОГИ БУТОВО.pptx», 27.07.2026):
   10 педагогов в 5 предметных категориях, фото + вырезки готовы. Осталось: **согласовать
   сжатые описания** (lead/text скомпонованы из презентации) и уточнить бейджи без стажа
   (Ялыманова, Данилова).
3. ✅ **Видео-отзывы получены и опубликованы 31.07.2026** — 15 роликов + постеры, формат mp4
   (самохостинг, не эмбеды). Подробности ниже, в разделе «Видео».
4. **Распорядок дня средней и старшей школы** (блок 10) — в макете он есть только у начальной.
5. **Названия выпадающих групп в шапке** (31.07.2026): клиент задал состав групп, но не их подписи —
   поставили «О школе» / «Обучение» / «Полезное». Если формулировки не подходят, меняется только текст
   кнопок `.header__group-btn` в `index.html` (и перегенерировать Геленджик).

**SEO — базовый набор сделан 28.07.2026** (домен `https://agb.lancmanschool.com`):
canonical, robots-мета, Open Graph + Twitter Card, `assets/img/og-cover.jpg` (сгенерирован),
`robots.txt`, `sitemap.xml`, JSON-LD `EducationalOrganization`. Детали — в `CLAUDE.md`, раздел «SEO».
✅ **JSON-LD дозаполнен реальными данными клиента 28.07.2026:** телефон `+7 (499) 346-73-50`,
адреса 3 филиалов (Ильинка 4 / Трубниковский 32 / Старобитцевская 15к1), `sameAs` — VK, YouTube,
Telegram (tracking `roistat_visit` вычищен). Тот же телефон стоит в шапке/меню/футере (снял заглушку).
Осталось по желанию: свой **баннер-превью** взамен `og-cover.jpg`; ссылки **Яндекс.Карты/2ГИС** в `sameAs`
(WhatsApp в `sameAs` не кладём — это click-to-chat, не профиль).
После деплоя: подключить Яндекс.Вебмастер + Google Search Console, отправить `sitemap.xml`.

**Разблокировано 19.07.2026 новым Figma-файлом** (платный аккаунт, лимита нет):
- ✅ Расписания всех трёх программ → `Tz/raspisanie.md`, блок 10 свёрстан.
- ✅ Промежуточные ширины 1600 и 1200 (секция «Визуал адаптивов» `1:21395`) → `--page-gap`, радиусы, ширины поп-апа формы.
- ✅ Полная карта мобильного фрейма 360 → `Tz/mobile-360.md`.
- ✅ Чистый экспорт мальчика для карточки «пробные 2 дня» вместо заглушки.

**✅ Мобильный проход 360–767 выполнен (20.07.2026).** Единый брейкпоинт `max-width: 767px`,
старые 560/600/480/375 убраны, «полудесктопных» состояний в 561–767 больше нет.
Сделано по блокам: вузы (сетка логотипов оказалась **4×3**, а не 3 в ряд), ЕГЭ-баннер,
«Приходите», форма, FAQ, «Программы», «О нас», персонал, факультативы, футер.
«Нет ДЗ» уже был свёрстан по мобильному фрейму и правок не потребовал.
Новое: `js/about.js` и `js/electives.js` + общий хелпер `createScroller()` в `main.js`
(карусели педсостава и факультативов), экспорт `assets/img/about/map-mobile.webp`
(у карты на телефоне **другой кроп**), общий компонент стрелок `.scroll-nav` в `layout.css`.
Детальные замеры — в `Tz/mobile-360.md`.

✅ **`pricing.css` и `reviews.css` приведены к канону** 21.07.2026 (вместе с резиновым
проходом), хотя блоки всё ещё закомментированы в `index.html`.

**Сверка рендера с макетом в Playwright (20.07.2026) — найдено и исправлено 10 дефектов:**

| # | Блок | Дефект | Причина |
|---|------|--------|---------|
| 1 | Нет ДЗ | вихрь не виден (ушёл за левый край) | нет `max-width: none` — глобальное `img { max-width: 100% }` резало 1224 → 360 |
| 2 | Приглашение | дети мельче и обрезаны снизу | то же: вырезка резалась 461 → 320 |
| 3 | Нет ДЗ | кнопка стояла **под** мальчиком | в разметке мальчик раньше кнопки; на телефоне порядок макета «текст → CTA → мальчик» — развели через `order` |
| 4 | О нас | «Педагогическийсостав» слиплось | `<br>` скрывали через `display: none`, а пробела вокруг него нет. Убрал `<br>` из разметки |
| 5 | О нас | карта цветная | в макете она обесцвечена (насыщенность 2.6–3.9 против наших 25–30) — добавлен `filter: grayscale(1)`. **Касается и десктопа** |
| 6 | Вузы | блок логотипов схлопывался в 20px | базовое `justify-items: center` + `aspect-ratio` + ленивые картинки: пока логотип не загружен, ширина 0. Поставлен `justify-items: stretch` |
| 7 | Вузы | логотипы мельче ячеек | добавлено `width/height: 100%` + `object-fit: contain` |
| 8 | Персонал | табы распирало на все 360 | правило ≤1279 уводит их в скролл отрицательным margin, на телефоне он не сбрасывался |
| 9 | Персонал | лента миниатюр 300 вместо 320 | не было выхода за правый край карточки (`margin-right: -20px`) |
| 10 | Классы | тёмное пятно в правом верхнем углу | на секции 360×1676 диагональ `135deg` вырождается — взят `--gradient-accent-v`; декор-вихрь прижат к низу (`contain`), иначе `cover` раздувал его в разы |

Плюс добавлена подсветка второй строки примечания в hero (нода `1:8338`) — её не было;
как и маркеры в «О нас», она есть **только** в мобильном макете.

**Проверено после правок:** горизонтальной прокрутки нет ни на 360, ни на 767; обе новые
карусели листают ровно по карточке (шаг 360 и 190) и гасят крайние кнопки; ошибок в консоли нет.

✅ **Активная миниатюра персонала на синей подложке — решено 27.07.2026.** Фон вырезан
нейросетью `rembg` (u2net) из фото презентации, миниатюры лежат RGBA-вырезками — активная
читается синей. (Та же проблема у кружков педсостава в «О нас» пока остаётся: там фото
без альфы, ждём вырезки.) Порядок логотипов вузов на телефоне в макете иной, чем на десктопе, —
не менял, ждём подтверждения состава и прав на логотипы.

**Найдено при пиксель-сверке FAQ (19.07.2026), выходит за рамки блока — решить отдельно:**
1. ⚠️ **Зазор между секциями-карточками в макете — 20px, у нас 40.** Замеры по `1920.png`:
   отзывы → FAQ (8588 → 8609) и персонал → приглашение (7336 → 7357) дают ровно 20.
   У всех секций стоит `padding-block: 20px`, то есть по 20 с каждой стороны = 40.
   Правка глобальная (`20px` → `10px` в ~9 файлах блоков), поэтому не делалась вместе с FAQ.
2. ✅ **Декор в `ege-banner.css`** — `max-width: none` у вихря уже стоит (проверено 20.07.2026),
   пункт закрыт.
3. ✅ **`container-type` в `universities.css`** — исправлено 21.07.2026 в ходе резинового
   прохода (перенесён на `.container`, значения волны пересчитаны ×0.93617). См. раздел
   «Резиновая вёрстка» ниже.

## 🫧 Резиновая вёрстка — проход 21.07.2026 (выполнено)

Задача: чтобы страница выглядела корректно **на любой ширине и высоте**, а не только
в четырёх контрольных точках. Модель — гибридная: текст интерполируется `clamp()`
между кеглями макета, раскладка задана долями, декор — в `cqw`. Подробности и токены —
в разделе «Резиновая вёрстка» в `CLAUDE.md`, замеры фреймов — в `Tz/adaptive-1600-1200.md`.

**Что сделано:**
1. **`base.css`** — ступенчатые `--fs-*` заменены кусочно-линейными `clamp()`;
   заведены `--gap-cards` и `--card-pad`; `--page-gap`, `--radius-btn`, `--radius-card`
   стали резиновыми. Медиазапросы 1600 и 1200 из `base.css` удалены целиком.
2. **`layout.css`** — `container-type: inline-size` объявлен один раз на
   `main .container, footer .container` (не на `body` и не на шапку — там `position: fixed`
   у мобильного меню); отступы кнопок сведены из трёх ступеней в одну формулу.
3. **Блоки 1–5 и форма** — интерполяция по реальным фреймам 1600/1200.
   Три ступени ширины поп-апа формы (1405/1165/879) заменены одной строкой
   `min(1405px, 73vw)`: это одна и та же доля во всех трёх фреймах.
4. **Блоки 6–17** — пропорциональная резина от 1880. Самое крупное: в `staff.css`
   px-сетка `107.22 | 70.85 | 472.04 | …` переведена в доли `fr` (плюс восьмая колонка —
   макетный остаток 85.09 справа), благодаря чему **ступень 1400 удалена**: композиция
   макета держится до 1025.
5. **Высоты** — все фиксированные `min-height` (hero 695, педсостав 603, бенто 287/289/909,
   тарифы 648, инфо-карточки 264) стали резиновыми минимумами; блок растёт, если текст
   не помещается. Модалка формы получила порог по высоте `max-height: 860px`.
6. **Брейкпоинты: было 12 порогов, стало 4** — 1280 / 1024 / 768 (точечно) / 767
   плюс один по высоте.

**Попутно закрыты два давних дефекта из этого файла:**
- ✅ `container-type` в `universities.css` висел на карточке с `padding: 60` (единица
  считалась от 1760 вместо 1880, фон был смещён на 6%). Перенесён на `.container`,
  значения волны пересчитаны ×0.93617. Откладывать было больше нельзя: `--card-pad`
  стал резиновым, и «подгонка под неверную базу» поехала бы с шириной экрана.
- ✅ `pricing.css` и `reviews.css` приведены к общим токенам и канону брейкпоинтов —
  при включении блоков отдельный адаптивный проход не потребуется.

**Проверено в браузере** (свип 21.07.2026): 1920 / 1700 / 1600 / 1440 / 1280 / 1100 /
1024 / 768 / 600 / 360 + короткий экран 800×500.
- Горизонтальной прокрутки нет ни на одной ширине; все «вылеты» элементов — это либо
  декор внутри блоков с `overflow: hidden`, либо карточки внутри каруселей.
- **Контрольные точки совпали с макетом точно.** На 1920: панель hero 1880×695,
  карточка персонала 1880×540, h1 96, поле 20, `--card-pad` 60. На 1600 (фрейм `1:22821`):
  панель 1580×690 от x=10, радиус 30, внутреннее поле 50, аттестат padding 20,
  ряд инфо-карточек от x=30 с зазором 10, CTA `14/30` радиус 10, h1 64.
  ⚠️ Заметьте: часть этих значений (радиус кнопки 10, поле аттестата 20) в вёрстке
  **раньше не соблюдалась** — теперь 1600 соответствует макету, чего не было.
- Интерактив на 1440 и 360: табы аттестата и расписания меняют контент, слайдер
  персонала листает, аккордеон держит открытым один пункт, обе карусели листают
  ровно по карточке (шаг 190 и 360), бургер-меню раскрывается на весь экран,
  форма на телефоне фуллскрин, на 800×500 крестик доступен. Ошибок в консоли нет.

**Найденный дефект, исправленный по ходу:** на 1440 лента миниатюр персонала подрезала
четвёртую миниатюру — ширина миниатюры считалась от колонки-доли, а высота ленты
в `cqw`, и они расходились. Обе привязаны к одной единице (`4.255cqw` и `18.617cqw`).

⚠️ **Осталось на согласование клиента (этим проходом намеренно не тронуто):**
- **Кегль расписания на телефоне** — на 360 плашки 7px, на 600 — 11.7px. Это прежний
  компромисс (в макете 4.39px), вопрос из списка ниже так и не закрыт. Варианты те же:
  оставить, вернуть кегль макета или увести расписание в горизонтальную прокрутку.
- **Высота шапки** (88 против 61 в макете) — источник правды не выбран.
- **Зазор между секциями 40 вместо макетных 20** — сквозная правка ~9 файлов,
  к резиновости отношения не имеет; токен `--gap-cards` для неё готов.

**Опечатки макета на согласование** (в вёрстку перенесены исправленными):
«пристижные» → «престижные» · «Гостинный двор» → «Гостиный двор» · «весь материал отрабатываются и закрепляют» → «отрабатывается и закрепляется» · «к егэ и огэ» → «к ЕГЭ и ОГЭ» · «Выберете школу» → «Выберите школу».

**Вопросы по мобильному макету (20.07.2026) — нужен ответ клиента:**
1. ⚠️ **Сетка расписания на телефоне нечитаема.** В макете (`1:14373`) названия дней идут
   кеглем **5.12px**, а предметы — **4.39px**: десктопная таблица просто уменьшена в 0.366 раза
   (167 → 61.07). По растру `Мобайл целый.png` текст плашек сливается в серое пятно.
   В вёрстке геометрия макета сохранена (5 колонок, шаг, радиусы), но кегль поднят до читаемого
   и растёт с шириной экрана — из-за этого плашки выше макетных и карточка выше 625px.
   **Варианты:** (а) оставить как сделано; (б) вернуть кегль макета 1-в-1; (в) уводить
   расписание в горизонтальную прокрутку при нормальном кегле.
2. ✅ **Кегль заголовков блоков — решено 20.07.2026: везде одинаковый, 32.**
   В макете разнобой (32 у «выберите класс», вузов, приглашения, персонала, FAQ,
   факультативов; 28 у «О нас», аттестата, ЕГЭ-баннера, тарифов) — выровняли по 32.
   Побочный эффект: ЕГЭ-баннер вырос с 545 до 592 (заголовок с 5 строк до 6),
   аттестат и вузы — на 8–14px. У ЕГЭ сняты ручные переносы, иначе повисали
   одиночные слова.
3. **Подсветка-маркер под заголовками карточек «О нас»** есть только в мобильном макете,
   на десктопе её нет. Сделано по макету (только ≤767) — так и оставляем?

**Прочие вопросы на согласование:** шапка (в Figma лого + 8 пунктов, у нас 7 без лого — оставили как есть по скрину клиента; **и высота: в макете 61px, у нас 88px** — какой источник главнее, макет или скрин клиента?); бейдж педагога в слайдере («образование» из макета заменён на реальный предмет); новый цвет `#CCE8FF` — сверить с `Цвета.png` (подтвердился как цвет плашки «литературное чтение» в расписании); в блоке 10 — пустые слоты в середине колонок и неунифицированные названия предметов (см. `Tz/raspisanie.md`); заголовки секций на мобилке разного кегля (28 и 32).

**По ходу:** чистые экспорты вместо `-tmp`-вырезок (мальчик без впечатанной кнопки, дети для баннера, лента в модалке формы); логотипы вузов; контент тарифов; расписания средней/старшей; карта; ссылки Яндекс/2ГИС; политика + согласие.

**Получено:** цвета, типографика, состояния кнопок/эффектов/формы, мобильное меню, Onest (самохост), лого цветное и белое, фото педагогов и руководителя, карта, **все 9 ответов FAQ**, макет формы, **фавиконки** (набор realfavicongenerator, 21.07.2026).
## ✍️ Типографский проход по всем текстам (21.07.2026, выполнено)

Задача: привести весь видимый текст к нормам русского набора, не меняя смысла
и не сдвигая пиксель-выверенную геометрию. До прохода неразрывных пробелов на весь
`index.html` было **9**.

**Что сделано:**
- **Неразрывные пробелы** — после коротких предлогов и союзов, между числом и словом
  («2 дня», «12 человек», «стаж 35 лет», «до 17:00»), в сокращениях («гос. образца»,
  «уч. год», «№ Л035-…»), **перед** длинным тире, в разрядах чисел («25 000», «7 000»).
- **Пропущенные пробелы после сокращений:** «уч.год» → «уч. год», «гос.образец» → «гос. образец».
- **Единое написание названия — «Моя школа LS»** («школа» со строчной), в кавычках-ёлочках,
  когда это имя собственное: «В «Моей школе LS» нет домашних заданий»,
  «Приходите познакомиться с «Моей школой LS»», «© 2026 «Моя школа LS»».
  Раньше в трёх местах стояло «Моя Школа LS» с прописной.
- **Ёфикация** текстов FAQ и таба 4 «Аттестата»: «ребенок» → «ребёнок», «остается» → «остаётся»,
  «дает» → «даёт», «вовлеченность» → «вовлечённость», «вперед» → «вперёд», «В чем» → «В чём».
- **Мелочи:** рейтинги в футере «5.0» → «5,0»; разделитель распорядка дня `|` → `·`;
  «Мини классы» → «Мини-классы» (таб 2 «Аттестата»).
- **CSS:** в `base.css` добавлено `p { text-wrap: pretty }` — браузер сам не оставляет
  висячее слово в конце абзаца. Заголовкам не дано: у них переносы расставлены вручную `<br>`.
- Правки внесены и в закомментированные блоки «Тарифы» и «Отзывы» — при включении
  отдельного прохода не потребуется.

**Техника:** в `index.html` — сущность `&nbsp;`; в данных JS — **литеральный символ U+00A0**,
потому что часть строк вставляется через `textContent` (заголовок таба в `attestat.js`,
ответ в `faq.js`, имя и бейджи в `staff.js`, распорядок в `programs.js`), где сущность
вывелась бы как текст «&nbsp;». В шапках этих файлов оставлены предупреждения:
в редакторе такой пробел визуально неотличим от обычного.

**⚠️ Пять неразрывных пробелов снято обратно** — они добавляли строку и ломали геометрию.
Правило прохода: **геометрия важнее идеального nbsp**. В коде каждое место помечено
комментарием. Проверено подбором в браузере (снимали по одному и мерили высоту):

| Где | Что вернули на обычный пробел | Что было бы |
|-----|-------------------------------|-------------|
| «Выберите класс», карточка «7 класс» | «по различным» | текст на 4-й строке, вся сетка +36px на 1280 |
| «Выберите класс», «Экстернат» | «и согласования» | то же (карточки в гриде равной высоты) |
| «Приходите познакомиться» | «с «Моей школой LS»» | баннер 523.6 → 552.4 на 375, маркер первой строки едет |
| Футер, реквизиты | «по телефону» | футер 977.5 → 994.3 на 375 |
| FAQ, вопрос 2 | «и государственной» | вопрос на 4-й строке, карточка 1236 → 1260 на 375 — **низ вихря обрывается по плотной части** |

**Проверка (Playwright, 1920 / 1280 / 768 / 375):** сняты замеры «до» и «после» по 16 блокам
+ высота карточки «Аттестата» на всех 5 табах. После снятия пяти nbsp все величины совпали
с исходными (единственное отличие — футер на 768: 855.6 → 854.4, доли пикселя, число строк
то же). Прокликаны табы «Аттестата» и расписания, аккордеон FAQ, слайдер персонала, форма.
Артефактов «&nbsp;» в виде текста в DOM нет.

**На согласование клиенту** (правки его текстов, смысл не менялся):
1. Ёфикация текстов FAQ и «Аттестата».
2. Единое написание «Моя школа LS» вместо «Моя Школа LS» + кавычки-ёлочки вокруг названия.
3. «Мини классы» → «Мини-классы».
4. Рейтинги «5,0» вместо «5.0» (по-русски дробный разделитель — запятая, но сами
   Яндекс и 2ГИС рисуют точку — вернуть, если важно совпадение с сервисами).

**Найдено попутно, к типографике не относится:**
- 🐞 **Форма никогда не отправляется.** В `js/form.js` `phoneDigits()` считает все цифры
  значения вместе с кодом страны («+7 (916) 123-45-67» → 11 цифр), а `validate()` требует
  ровно 10 (`js/form.js:88`) — при полностью заполненном номере всегда показывается
  «Укажите номер полностью». Воспроизведено настоящим набором с клавиатуры. Правка
  однострочная, но выходит за рамки типографского прохода — не делал.
- Колонка реквизитов в футере на 375 шириной ~80px: слова рвутся по слогам
  («информацион-ный»). Существовало до прохода, лечится вёрсткой футера.

## 🏙️ Мультигород: Москва / Геленджик (30.07.2026, выполнено)

Задача клиента: выбор города Москва / Геленджик. Москва — как есть; Геленджик — в hero
1 чипсик локации вместо 3, другая карта; остальное как в Москве (футер/форму/JSON-LD
тоже переключаем — решение клиента).

**Реализация** (детали — в CLAUDE.md, раздел «Мультигород»):
- Две страницы с раздельными URL (SEO): `/` (Москва) и `/gelendzhik.html`. Один URL с
  JS-переключением не подходит — Геленджик не индексировался бы.
- `gelendzhik.html` **генерируется** из `index.html` скриптом `build-cities.py`
  (index.html — единственный источник правды; правим только его → `python3 build-cities.py`).
  22 проверяемых замены: hero-чипсики, карта+пины, футер-адреса, радио формы, SEO-теги, JSON-LD.
- Переключатель городов — тумблер `.city-switch` в шапке (десктоп) и в бургер-меню.
- Карта Геленджика — `assets/img/about/map-gelendzhik.webp` (кроп Яндекс.Карт,
  Красногвардейская 38 = «Студия Анны Деменино»), серая тем же CSS-фильтром, что Москва;
  1 пин выверен в браузере (десктоп 45.7/59, мобилка 42/45).
- SEO: у каждой страницы self-canonical + свои title/description/OG/JSON-LD с адресом
  города; `sitemap.xml` — 2 URL; одна property Вебмастера/GSC на обе.

**Проверено в Playwright** (обе страницы, 1920/1501/1500/375): тумблер, 1 чипсик hero,
пин на здании (десктоп 49.1%/45.7%, мобилка 48.2%/46.5%), бургер с тумблером, JSON-LD валиден.

**Правки 30.07.2026 (после просмотра клиентом):**
- Шапка: порог бургера **оставлен на 1280** (клиент хочет полное меню на десктопе).
  Чтобы полное меню с тумблером влезало до 1280, зазоры `.header__inner`/`.header__nav`
  сделаны резиновыми `clamp(24px, 2.5vw - 8px, 40px)` + резиновый отступ тумблера +
  `nowrap` пунктам. Ниже ~1280 всё равно бургер (6 пунктов+тумблер+телефон+CTA не влезают,
  контент-минимум ~1226); клиент решил **оставить все 6 пунктов** (мёртвый `#advantages`
  «Преимущества» не убирали).
- Hero: пол высоты панели поднят 470→**540** — на узком десктопе (1025–1327) белый стикер
  CTA свисал на карточки; теперь зазор ~43px.
- Инфо-карточки: текст растянут `max-width: max(72%, 240px)` — на узком десктопе колонка
  не рвётся в лапшу, заполняет карточку (72% и макет на 1920/1600 не тронуты).

**На согласование клиенту:**
1. Карта Геленджика собрана из присланного скриншота (962px) — на retina чуть мягче
   московской (1800px). Если пришлют скрин крупнее — станет резче.
2. Разнести ли контент между городами глубже (тексты, отзывы, цены), или Геленджик =
   Москва с другим адресом/картой (сейчас так).


## 🎬 Видео на сайте (31.07.2026, выполнено)

Клиент прислал 17 роликов-отзывов (`otzivi/`) и 3 обзорных (`obzor/`).

**Что сделано:**
- **Блок 14 «Отзывы»** — 15 плиток с постерами вместо пустых заглушек, дети и родители
  вперемешку, листаются страницами (8 на экран на 1920, дальше 6/4/1 по ширине).
- **Блок 7, карточка «Педагогический состав»** — заменена на слайдер из 3 обзорных роликов;
  заголовок, подпись и все 4 педагога с фото и бейджами удалены. Первым идёт «Средняя
  и старшая школа», как просил клиент.
- Треугольник play отцентрирован оптически (жалоба «стрелка не отцентрирована»).
- Ленивая загрузка: все 18 постеров + 4 существовавших изображения ниже первого экрана.

**⚠️ Ключевое: ролики пришлось перекодировать, иначе Safari их не открыл бы.**
Все файлы — выгрузки с YouTube: аудио везде Opus, у 6 отзывов видео VP9, всё в MP4.
Проверено в движке WebKit: `avc1+opus` и `vp09+opus` → `canPlayType` возвращает пусто,
то есть **не играет**; наш выход `avc1+mp4a.40.2` → `probably`. Chrome оба варианта
играет, поэтому проверка только в Chrome ничего бы не показала. Перекодировано в
H.264 + AAC, 720p, `+faststart`. Итог: 440 МБ исходников → **180 МБ** на проде.

**Проверено:** воспроизведение со звуком в Chrome и WebKit на 5 роликах (в т.ч. на всех
трёх «тяжёлых» VP9-исходниках); раскладка и листание на 1920/1280/1024/375 без
горизонтальной прокрутки; ошибок в консоли нет.

**Что решил клиент по ходу:**
- Качество 720p (~180 МБ) вместо 540p и вместо ютуб-эмбедов.
- Два файла из `otzivi/` — «Европейский подход и лучшее из российской школы» и
  «Только Ланцман скул» — **не выкладывать**: это промо-нарезки, а не личные отзывы.
- Плитки отзывов — с постерами, но **без подписей** с именем и ролью (как в макете).
- Заголовок карточки педсостава — убрать совсем; видео во всю карточку, без полей.

**На согласование клиенту:**
1. В имени файла «Фицнер **Ларписа**» — похоже, опечатка; в подписи для скринридера
   поставил «Лариса». Если нет — поправить в `js/reviews.js`.
2. Подпись третьего обзорного ролика дал как «1 сентября в школе», без «2023»: год
   в 2026-м смотрится устаревшим. Исходное имя файла — «1 сентября 2023 в Lancman School».
3. Ролики брендированы старым «Lancman School» (заставки, плашки с титрами, логотип
   на стене), а сайт — «Моя школа LS». Расхождение видно на постерах. Не вёрстка,
   но стоит решить: оставляем как есть или клиент перевыпустит ролики.
4. Фото педагогов `assets/img/about/{ryaboshapka,mamaeva,abdulmedzhidova,samoylova}.webp`
   после замены карточки стали сиротами. Файлы **не удалял** — они независимы от фото
   тех же людей в блоке 12 (`assets/img/staff/`, другой кроп). Удалять или оставить
   на случай возврата карточки педсостава — решение клиента.
5. Исходные папки `otzivi/` и `obzor/` (~510 МБ) добавлены в игнор `sftp.json`, на прод
   не уходят. Держим их в проекте или переносим в архив вне репозитория?
