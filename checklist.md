# Чеклист переработки по ТЗ от 06.09.2026

Ветка **`tz-2026-09`**. План: `~/.claude/plans/graceful-pondering-flamingo.md`.
Исходное ТЗ: `Аудит_и_ТЗ_онлайн-школа_LS (1).md`.
Между фазами `/clear`; состояние — только в этом файле.

## Решения клиента (закрывают §24 ТЗ)

1. Период у всех цен — «/ мес», включая 29 000, 39 000, 35 000 и 45 000 ₽.
2. Начальная школа заканчивает в 14:15. ⚠️ В коде времени окончания начальной школы вообще нет —
   при вёрстке блока «Как проходит учебный день» пишем 14:15.
3. Юридические ссылки пока остаются `#`.
4. Способ работы: делаем всё по документу дословно, спорное помечаем и отдаём на ревью.

## Фазы

- [x] **Фаза 1. Сквозные текстовые правки** — 07.09.2026
- [x] **Фаза 2.** Блоки 1–3: hero, преимущества, аттестат (снять вкладки) — 07.09.2026
- [x] **Фаза 3.** Блоки 4–5: новый `#who-fits`, `#programs` → `#school-day` + слияние `no-homework` — 07.09.2026
- [x] **Фаза 4.** Блоки 6–7: `#classes` (3 ступени + экстернат), слияние `#about` и `#staff`,
      вынос `#universities` в `disabled-sections.html` — 07.09.2026
- [x] **Фаза 5.** Блоки 8–9: новый `#formats`, оживление `#pricing` с реальными тарифами — 07.09.2026
- [x] **Фаза 6.** Блоки 10–12: `#electives` → `#additional`, отзывы по §15, `invite` → `#trial` — 07.09.2026
- [x] **Фаза 7.** Блоки 13–15: FAQ (11 вопросов), новый `#consultation`, футер — 07.09.2026
- [ ] **Фаза 8.** Сквозной слой: меню, липкий CTA, состояние, аналитика, доступность, чеклист §23

## Что сделано в фазе 1

- Размер класса приведён к «до 15 учеников в классе»: `meta description`, `og:description`,
  `twitter:description`, JSON-LD, `data-class-size` (12 → 15), пилюля вкладки и чип в блоке «О нас»,
  четыре пилюли в `disabled-sections.html`. Запасное значение в `js/attestat.js` тоже 15.
- Пробные дни приведены к «2 пробных дня» в девяти местах, единственного числа не осталось.
- Проверено: синтаксис всех JS, рендер вкладок аттестата, отсутствие горизонтальной прокрутки.
  Единственная ошибка консоли — сертификат `hdrc.yandex.net`, к проекту отношения не имеет.

## Что сделано в фазе 2

- **Hero (§5).** Подзаголовок, строка «Открыт набор на 2026/27 учебный год», CTA «Получить
  консультацию и 2 пробных дня», подпись под кнопкой `.hero__cta-note`. Стикер «бесплатные
  занятия» снят: §5 перечисляет содержимое экрана закрытым списком.
- **Преимущества (§6).** Пять карточек вместо четырёх, среди них «Государственная лицензия
  № Л035-01298-77/00179753». Бездоказательное «каждый 5-й выпускник» убрано. Раскладка
  5 / 3+2 / 2 колонки, лицензия на телефоне во всю ширину. Пол 240px у текста снят —
  на 1280 при пяти колонках он выпихивал текст за карточку.
- **Аттестат (§7).** Вкладки удалены целиком: разметка, 172 строки `js/attestat.js`
  и стили пиллов, бабблов, фото-табов и подзаголовков. Осталось три факта, пояснение
  «подробности — на консультации» и CTA «Узнать подробнее».
  Коллаж стал `<button>` и открывается в общем лайтбоксе (`window.openLightbox` из `main.js`),
  подсказка «Нажмите, чтобы рассмотреть». Кнопка растянута на медиа-колонку (`inset: 0`),
  иначе фокус с клавиатуры вставал бы на полоску нулевой высоты.
- Проверено на 1920 / 1024 / 768 / 390: горизонтальной прокрутки нет, консоль чистая,
  лайтбокс открывается и закрывается. Версии `?v=` подняты у изменённых файлов.

## Что сделано в фазе 3

- **Кому подходит (§8).** Новая секция `#who-fits` (`css/who-fits.css`) сразу после аттестата:
  H2, подзаголовок, шесть карточек 3×2 / 2×3 / вертикаль, CTA «Подобрать формат для нашей семьи».
  Карусели нет — карточки всегда лежат сеткой.
- **Учебный день (§9).** `#programs` → `#school-day`, файлы переименованы через `git mv`
  (`css/programs.css` → `css/school-day.css`, `js/programs.js` → `js/school-day.js`),
  все селекторы `.programs*` → `.school-day*`. Внутри: H2 + подзаголовок, четыре шага
  (4 / 2×2 / вертикаль), абзац про самостоятельную работу, плашка «Все записи уроков…»
  с CTA «Посмотреть пример расписания», две строки нагрузки (14:15 и 16:15),
  «Пример учебной недели» с табами «1–4 класс · 5–8 класс · 9–11 класс».
- **Мобильное расписание** больше не сжимается: появился переключатель дней (пять кнопок,
  зона нажатия 44px), виден один день во всю ширину. На планшете лента лежит
  в `.school-day__scroller` с горизонтальной прокруткой — при 1024 и 768 она пока не нужна,
  сетка помещается.
- **Блок `no-homework` удалён**, `css/no-homework.css` тоже: его карточка стала
  «дополнительной плашкой» из §9 (`.school-day__plate*`), текст про самостоятельную работу
  переехал в `.school-day__selfwork`.
- **Синяя карточка «2 пробных дня»** из бывшего блока программ вынесена
  в `disabled-sections.html` (её тему закрывает блок 12 `#trial`, фаза 6).
  ⚠️ Стили `.programs__promo*` удалены — возвращать только вместе с `git show 2738907:css/programs.css`.
- Пустые слоты расписания теперь добираются до самого длинного дня **ступени**, а не всегда
  до шести: два мёртвых ряда внизу ушли. Выравнивание высоты карточки между ступенями
  (`syncCardHeight`) сохранено, на телефоне отключено.
- Три якоря `#programs` в шапке, бургер-меню и футере перенацелены на `#school-day`
  с подписью «Как проходит обучение» (§4). Остальное меню — фаза 8.
- Проверено на 1920 / 1280 / 1024 / 768 / 390: горизонтальной прокрутки нет, консоль чистая,
  переключение ступеней и дней работает, выбранный день переживает смену ступени,
  CTA плашки прокручивает к `#week-example` с учётом липкой шапки.

## Что сделано в фазе 4

- **Выберите класс (§10).** Восемь карточек-классов → три ступени («Начальная / Средняя /
  Старшая школа» с подписями 1–4 · 5–8 · 9–11) и широкая розовая карточка «Экстернат».
  Кнопки из карточек убраны, внизу один CTA «Узнать об обучении в вашем классе».
  Раскладка 3 / 2+1 / accordion. Экстернат намеренно не выбирается: это не ступень
  и тарифа у него нет.
- **Состояние (§20).** В `js/main.js` появился `window.schoolChoice`
  (`school_stage`, `education_format`, `cta_location`) и хелперы `setSchoolStage`
  / `setEducationFormat`. Клик по любому CTA пишет `cta_location` — из `data-cta-location`
  или из id секции. Блок форматов пятой фазы и аналитика восьмой пишут туда же.
  ⚠️ В CRM эти поля не уходят — см. пункт 1 списка на ревью.
- `js/school-day.js`: стартовое включение первой вкладки стало «тихим». Раньше оно
  записывало ступень «начальная» при загрузке страницы, и форма получила бы выбор,
  которого родитель не делал.
- **Команда школы (§11).** `#about` и `#staff` слиты в один блок «За ребёнком стоит
  команда школы»: подзаголовок, четыре карточки ролей (преподаватели, куратор, психолог,
  руководство), карточка «Лицензия и официальный статус» с четырьмя пунктами и сканом,
  шесть профилей и кнопка «Посмотреть всех преподавателей» (открывает остальные 21).
  Планшет — роли 2×2 и профили по два, телефон — роли accordion, профили горизонтальной
  каруселью на scroll-snap (без JS).
- `js/staff.js` → `js/about.js` (`git mv`): данные 27 сотрудников сохранены дословно,
  переписан только показ. `css/about.css` переписан целиком, `css/staff.css` удалён.
- Со страницы ушли: чипы-повторы («живые уроки», «до 15 учеников», «запись занятия»,
  «электронный журнал») по §11, плашка «аттестат московской школы-партнёра» и «московская
  медаль» по §3, карточка «Руководящий состав».
- **`#universities` вынесен** в `disabled-sections.html`: в списке 15 блоков ТЗ его нет,
  а «самые престижные вузы» — бездоказательное утверждение из §3. `css/universities.css`
  остаётся подключённым (общее правило файла отключённых секций).
- Проверено на 1920 / 1280 / 1024 / 768 / 390: горизонтальной прокрутки нет, консоль чистая,
  выбор ступени пишется в `school_stage` и уточняет тему заявки, при загрузке ступень пуста,
  кнопка разворачивает 6 → 27 профилей, оба accordion открываются, зоны нажатия 86 и 62px.

## Что сделано в фазе 5

- **Выберите формат (§12).** Новая секция `#formats` сразу перед `#pricing`
  (`css/formats.css`, `js/formats.js`): сравнение по шести критериям, два CTA
  и строка «Не уверены, какой вариант выбрать?». Одна разметка на три раскладки —
  десктоп таблица, планшет две колонки-карточки (подпись критерия из `data-crit`
  через `::before`), телефон одна колонка + «Сравнить оба варианта» на весь экран
  (`position: fixed`, z-index 90: выше шапки 50, ниже меню 100 и формы 110).
- Переключатель форматов только **показывает** колонку; формат записывается
  в состояние по нажатию CTA — иначе в форму уходил бы вариант, который родитель
  просто пролистал. CTA прокручивает к `#pricing` и подсвечивает там свой тариф.
- **Стоимость (§13).** `#pricing` вернулся из `disabled-sections.html` с реальными
  тарифами: три ступени, общая часть, по две карточки формата, шесть цен из §13
  («/ мес» у всех — решение клиента). `js/pricing.js` держит данные, `css/pricing.css`
  переписан целиком (заглушки «Тариф 1/2/3» удалены, старая версия — `git show 416a29f:css/pricing.css`).
- **Связка блоков.** `js/main.js` рассылает `CustomEvent('school-choice')`, добавлен
  `window.setPrice`. Ступень, выбранная в «Выберите класс», переключает тариф;
  формат, выбранный в `#formats`, подсвечивает карточку, и наоборот. CTA тарифа
  пишет `school_stage`, `education_format`, `price` и `cta_location=pricing` (§13).
- Заголовок общей части — «Общее для всех тарифов», формулировка самого ТЗ
  (в §13 это служебный подзаголовок раздела; если он не задумывался как текст
  страницы — переименовать на ревью).
- ⚠️ Заведена **неканоничная ступень 800px**: §13 требует перелом ровно там
  («при ширине менее 800 px — одна под другой»). Канон проекта — 1280/1024/768/767.
- Проверено на 1920 / 1280 / 1024 / 790 / 390: горизонтальной прокрутки нет,
  консоль чистая, accordion тарифов открывает по одному, цена и CTA видны до раскрытия,
  полноэкранное сравнение открывается и закрывается по Escape с возвратом фокуса,
  зоны нажатия 44px.

## Что сделано в фазе 6

**Блок 10 «Дополнительные занятия» (§14).** `#electives` → `#additional`:
`css/electives.css` → `css/additional.css`, `js/electives.js` → `js/additional.js` (через `git mv`,
история файлов сохранена). Прежние восемь факультативов в бенто-раскладке заменены на три карточки
(ОГЭ/ЕГЭ, языки и элективы, профориентация) и раскрывающийся по кнопке полный список — пять
категорий из §14 дословно, 20 направлений. Ниже — строка «Занятия из полного списка оплачиваются
отдельно» (§14 велит показывать их как отдельные платные возможности) и подпись про доступность
не только ученикам школы. Один CTA «Узнать подробнее», `cta_location=additional`.
Раскладка: 3 карточки → 2 → 1, полный список 3 → 2 → 1 колонки, на телефоне и карточки,
и категории — accordion.

**ЕГЭ-баннер снят.** Разметка `#ege-banner` переехала в `disabled-sections.html`: в списке 15 блоков
ТЗ его нет, а «70% выпускников сдают ЕГЭ выше 240 баллов» запрещено §3. Тема подготовки к экзаменам
осталась карточкой в `#additional`. `css/ege-banner.css` остаётся подключённым — правило
`disabled-sections.html`.

**Блок 11 «Отзывы» (§15).** Страничный пагинатор со стрелками заменён плоским списком:
на первом экране один крупный отзыв и три компактные рядом (сетка 4 колонки, крупная плитка 3×3 —
ряды сходятся без дыр), остальные 11 открывает кнопка «Показать ещё отзывы». Кнопка только
раскрывает список и лид-форму не открывает (проверено). Данные разведены на `name` и `status`:
имя и статус теперь видны без запуска видео. Планшет — ровная сетка 2×2, телефон — лента
по карточке на экран (85% ширины, виден край следующей) со счётчиком «N / 15»; кнопка там скрыта,
все 15 отзывов доступны свайпом. Постеры по-прежнему через `data-src` + `applyLazySrc`.
Подзаголовок заменён на текст §15.

**Блок 12 «Два пробных дня» (§16).** `css/invite.css` → `css/trial.css`, безымянная секция `invite`
стала `#trial` и переехала после отзывов. Тексты §16 дословно (H2, подзаголовок, строка про
консультацию), CTA «Записаться на 2 пробных дня», `cta_location=trial`. Раскладка баннера и замеры
по Figma не менялись — §16 просит ровно её; `kids.webp` / `kids-mobile.webp` на месте, подробных
правил пробных дней нет.

**Порядок секций приведён к ТЗ:** `#formats` → `#pricing` → `#additional` → `#reviews` → `#trial` →
`#faq`; порядок `<link>` и `<script>` в `index.html` выстроен по секциям, стили отключённых блоков
(`ege-banner`, `universities`) вынесены в конец списка. Три якоря «Факультативы» (шапка, мобильное
меню, футер) переведены на `#additional`.

**Расхождения с буквой ТЗ (на ревью):**
- §15 просит и «не более шести отзывов на первом экране», и «один крупный и три компактные рядом».
  Сделана вторая формулировка: видно 4 карточки (это ≤ 6). Шесть карточек в такой сетке дают
  либо дыру в ряду, либо вторую крупную плитку.
- Метки к отзывам (§15) остались пустыми — какая метка какому ролику соответствует, из кода
  не следует. Место под метку свёрстано (`.reviews__tag`), пункт 2 списка на ревью.
- «Занятия из полного списка оплачиваются отдельно» — формулировка моя: §14 требует показать
  их платными, но своего текста не даёт.

**Проверено** на 1920 / 1280 / 1024 / 768 / 390: горизонтальной прокрутки нет, консоль чистая,
список направлений закрыт при загрузке и открывается кнопкой, «Показать ещё отзывы» разворачивает
15 карточек и не открывает форму, счётчик на телефоне считает карточки при свайпе, accordion'ы
на телефоне закрыты.

## Что сделано в фазе 7

**Блок 13 «FAQ» (§17).** Прежние вопросы заменены целиком на одиннадцать из §17 — дословно,
правилась только типографика проекта. Старый набор раскрывал запрещённое §3 (документы,
порядок аттестаций, объём работы куратора); он остаётся в истории: `git show 9ab0d19:js/faq.js`.
Механика блока уже соответствовала §17 и не менялась: при загрузке закрыты все вопросы, открыт
максимум один, состояние в `aria-expanded`, у каждого вопроса `aria-controls` на свой ответ.
Заголовок собран в три строки со светлым «Ответы» — декоративная разбивка, в ТЗ её нет.

**Блок 14 «Финальная форма» (§18).** Новая секция `#consultation` после FAQ, с ней новые
`css/consultation.css` и `js/consultation.js`. Поля по §18: «Как к вам обращаться», «Телефон»
(маска `+7 (___) ___-__-__`), «Класс ребёнка» — список 1–11; скрытые `school_stage`,
`education_format`, `price`, `cta_location=consultation`; строка согласия, CTA «Получить
консультацию», экран успеха «Заявка отправлена». Ступень в скрытом поле выводится из выбранного
класса (1–4 / 5–9 / 10–11), формат и цена приезжают событием `school-choice` из блоков «Форматы»
и «Стоимость». Раскладка: десктоп — текст слева, форма справа; планшет — одна колонка, форма
640px; телефон — поля и кнопка во всю ширину, высота элементов 48–50px (§18, §21).

⚠️ **Клик по кнопке успешной заявкой не считается** (прямое требование §18): экран успеха
показывается только по событию от формы Битрикса (`b24:form:success` / `b24:form:submit`).
Если событие не придёт, человек остаётся в форме Битрикса, а ложного «отправлено» не будет.

**Блок 15 «Футер» (§19).** Навигация приведена к новой структуре: Преимущества, Как проходит
учебный день, Классы, О нас, Форматы обучения, Стоимость, Дополнительные занятия, Отзывы, FAQ,
Консультация — все десять якорей ведут в существующие секции (проверено). Рейтинги Яндекс и 2ГИС
перестали быть ссылками (`<a href="#">` → `<div role="img">`): §19 разрешает кликабельность
только при наличии реальных адресов. Логотип, данные ИП, лицензия, телефон, Telegram, WhatsApp,
ВКонтакте, юридическое предупреждение и копирайт сохранены. Ссылки на политику и согласие
остаются `#` — решение клиента, блокер публикации.

**Попутно найден и исправлен баг маски телефона.** `phoneDigits()` считал цифры вместе с семёркой
кода страны из маски, получалось 11 при проверке `=== 10`, и полностью введённый номер отбивался
как неполный. Правка внесена и в новый `js/consultation.js`, и в `js/form.js`, откуда код взят;
в модалке баг не всплывал, потому что её перекрывает форма Битрикса.

**Расхождения с буквой ТЗ (на ревью):**
- Тексты `#consultation` (заголовок, подзаголовок, подписи полей, сообщения об ошибках, экран
  успеха) — мои: §18 задаёт состав блока и поля, но своих формулировок не даёт.
- Отправка формы идёт не своим кодом, а передачей в click-форму Битрикса — пункт 1 списка
  на ревью. Поэтому заполненные скрытые поля в CRM не попадают, а цель «успешная заявка»
  сработает только после подключения формы онлайн-школы.
- Имя события об успешной отправке зависит от версии загрузчика Битрикса; слушаем оба варианта.
  Проверить на боевой форме, когда клиент даст её ID.
- Трёхстрочный заголовок FAQ с подсветкой слова «Ответы» — декор, в §17 такого требования нет.

**Проверено** на 1920 / 1280 / 1024 / 768 / 390: горизонтальной прокрутки нет, консоль чистая,
в FAQ 11 вопросов и при загрузке закрыты все, открыт всегда один; пустая форма даёт три ошибки
и не открывает Битрикс, фокус уходит в первое неверное поле; заполненная — открывает форму
и передаёт `cta_location=consultation`, экран успеха не показывается по клику и появляется
только по событию Битрикса.

## Найдено по ходу (учесть в следующих фазах)

- Варианта «14:30» в коде нет — сверять было не с чем, ставим 14:15 из ответа клиента.
- ~~Расписание (`js/programs.js:21`)~~ — распорядок начальной школы сохранён дословно
  в `js/school-day.js` (§9 велит), расхождение вынесено в список на ревью, пункт 6.
- ~~`js/attestat.js` (вкладка «Удобный формат») раскрывает запрещённое §3~~ — снято в фазе 2
  вместе с вкладками, хвостов в CSS и разметке не осталось (проверено grep).
- ~~`index.html:416` — «аттестат… московской школы-партнёра» и «московская медаль»~~ — убрано в фазе 4
  вместе со старыми карточками блока «О нас».
- Осиротевшие файлы после фазы 4: миниатюры `assets/img/staff/*-thumb.webp` (27 штук — лента
  миниатюр удалена), `assets/img/about/direktor.webp`, `assets/img/about/medal.webp`.
  Не удаляю до конца переработки: вдруг понадобятся в фазах 5–7. Прибраться в фазе 8.
  Папка `assets/img/staff/` имя не меняла — пути лежат в данных `js/about.js`.
- ~~Бездоказательная цифра «70% выпускников… 240 баллов» (ЕГЭ-баннер)~~ — снята в фазе 6
  вместе со всей секцией. «Каждый 5-й выпускник» убран в фазе 2.
- Осиротевшие после фазы 6: `css/ege-banner.css` подключён, но на странице блока нет (так велит
  правило `disabled-sections.html`). Если клиент подтвердит, что баннер не вернётся, — снять
  подключение и удалить `assets/img/ege/*` в фазе 8.
- Класс `.pill` был локальным для аттестата и удалён вместе с вкладками. Если в фазах 4–5
  понадобятся пилюли-переключатели (ступени, форматы), стиль писать заново в файле своего блока.
- Скрытые поля `school_stage`, `education_format`, `price`, `cta_location` заполняются,
  но в CRM не уходят — форма чужая (пункт 1 списка на ревью). Подставить их в форму
  и в аналитику — фаза 8.
- ~~Старый FAQ (`js/faq.js`) раскрывает документы, аттестации и объём работы куратора~~ — заменён
  целиком в фазе 7 на 11 вопросов §17.
- ⚠️ Кеш скрипта: правка `js/faq.js` без подъёма `?v` не доехала до браузера — страница показывала
  старые 9 вопросов. Поднято до `?v=3`. Правило: менял файл — меняй номер версии в `index.html`.
- Юридические ссылки в футере и в согласии `#consultation` — четыре штуки на `#`. Перед публикацией
  заменить разом (пункт 3 списка на ревью).

## Список на ревью клиенту (вёрсткой не закрывается)

1. Заявки уходят через чужую click-форму Битрикса офлайн-школы (`click/225/yn061r`). Скрытые поля
   и цель по факту успешной отправки не работают, пока клиент не даст ID формы онлайн-школы.
2. Метки к 15 видеоотзывам: в данных только имя и класс, содержание роликов нигде не записано.
3. Юридические ссылки — `#`.
4. `vk.com/lancman_school` в футере раскрывает бренд партнёра вопреки §3, но §19 велит ВК сохранить.
5. Расписание досталось от офлайн-школы — подтвердить актуальность для онлайн-формата.
6. **Распорядок дня начальной школы спорит сам с собой.** Сохранённая по §9 строка говорит
   «Уроки в эфире 9:30–14:00 (5 уроков)… Самоподготовка с куратором 15:30–16:30», а нагрузка
   из того же §9 — «4 урока в день» и окончание в 14:15 (ответ клиента). Сетка расписания тоже
   показывает четыре урока. На странице сейчас стоят обе версии, как написано в ТЗ.
   Нужен один правильный распорядок — и, если он есть, распорядки для 5–8 и 9–11 классов:
   у них блок распорядка пустой и скрывается сам.
