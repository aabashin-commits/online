# Онлайн-версия: отличия от офлайн-папки

Папка сделана командой `cp -a` из `Lendosi/скул офлайн/` (бывшая `school/`).
**Структура блоков, сетка, токены, брейкпоинты и адаптив не тронуты вообще** —
поменялись только тексты и то, что физически привязано к городу.

**Удалено из копии:** `gelendzhik.html`, `build-cities.py`,
`assets/img/about/map.webp`, `map-mobile.webp`, `map-gelendzhik.webp`.
Мультигорода здесь нет и не будет: у онлайн-школы одна страница.

**Что переделано относительно офлайн-версии:**

| Место | Было (офлайн) | Стало (онлайн) |
|---|---|---|
| Шапка | тумблер `.city-switch` Москва/Геленджик (2 экземпляра) | тумблера нет, разметка и CSS удалены |
| Hero, чипсы | Гостиный двор / Арбат / Бутово с иконкой `map-pin.svg` | Онлайн / 1–11 класс / Вся Россия и мир с иконкой `green checkmark.svg` |
| Hero, вырезка | пара `kid-left.webp` + `kid-right.webp` с перекрытием −7% | одна `kid-online.webp` — девочка с ноутбуком (13.08.2026) |
| «О нас», правая колонка | карта Москвы `.about-map` + 3 пина `.map-pin` | плитка `.about-online`: градиент, текст про московскую медаль, фото медали в углу |
| Футер | `.footer__addresses` — 3 адреса филиалов на Яндекс.Картах | `.footer__contacts` — Telegram / WhatsApp / ВКонтакте |
| Форма | `<legend>` «Выберите школу», радио с 3 филиалами | «Выберите ступень», радио Начальная / Средняя / Старшая |
| Блок 4 | пилла «Класс до 12 человек» | «Группа до 12 человек» (механика `data-class-size` + `{size}` сохранена) |
| JSON-LD | `areaServed: Москва` + массив `department` из 3 филиалов | `areaServed: Country Россия`, `department` удалён |
| `sitemap.xml` | 2 URL | 1 URL |

**Порог сворачивания шапки в бургер остался 1280.** Тумблер занимал ~220px, без
него в шапке стало свободнее; резиновые зазоры `clamp(24px, 2.5vw - 8px, 40px)`
у `.header__inner`/`.header__nav` оставлены как были — они просто перестали
упираться в минимум. Замер после правки: на **1281** полное меню, правый край CTA
на 1251 (запас 30px), горизонтального скролла нет; на **1280** — бургер, в меню
те же 7 пунктов.

**Плитка `.about-online`** (заменила карту, `css/about.css`). Изначально (13.08–
01.09.2026) внутри был заголовок «Учимся онлайн из любой точки», три белых чипа
и вырезка ученицы `girl-online.webp`, геометрия — фиксированный `aspect-ratio`
929/677 (677/677 на ≤767), взятый у карты, чтобы колонки не разъезжались.

Решение клиента 01.09.2026 заменило содержимое на блок про московскую медаль
«За особые успехи в обучении» (доп. баллы к ЕГЭ при поступлении в вуз):
заголовок + подзаголовок + абзац слева, фото медали в наградной коробочке
(`assets/img/about/medal.webp`) — абсолютом в правом нижнем углу, часть уходит
за оба края и срезается `overflow: hidden` (тот же приём, что `.license__img`).
Фиксированный `aspect-ratio` убран — из-за возросшего объёма текста высота
плитки, как у `.manager`/`.school-video`, теперь идёт по контенту.
`container-type: inline-size` остался — размеры внутри по-прежнему в `cqw` от
ширины плитки. `girl-online.webp` осталась на диске сиротой.

### Вырезка hero `kid-online.webp` (13.08.2026)

Заменила пару вырезок офлайн-версии: теперь в hero **одна** картинка — девочка
с ноутбуком, и в неё **вшиты четыре плашки** («удобство», «живые занятия и в записи»,
«гибкость», «гос аттестат»). Отдельными элементами плашек нет, текст в них
из вёрстки не правится — только перерисовкой картинки.

⚠️ **Доли ширины на планшете и телефоне пересчитаны** (`min(70%, 460px)` на ≤1024
и `86%` на ≤767 вместо прежних `min(48%, 320px)` и `50%`): старые значения делили
ширину между ДВУМЯ вырезками, и одна картинка выходила вдвое мельче нужного.

⚠️ **`.hero__kids` подняты на `bottom: 30px`, а не 0.** Инфо-карточки наезжают
на низ панели на 110px, и при `bottom: 0` нижняя плашка «гос аттестат» (её пилюля
кончается на ~87% высоты вырезки = 96px над низом панели) уходила под них
и обрезалась. Подъём выводит плашку из зоны наезда с запасом ~16px, при этом
прямой срез картинки снизу остаётся спрятан под карточками — иначе была бы видна
линия обрезки.

⚠️ **У исходника была «грязная» альфа:** внутри 253 вместо 255, а снаружи 1–3
вместо 0, и RGB под этими пикселями чёрный — на синем фоне это давало тёмную дымку
по краям, а при ресайзе чёрный затекал в контур. Лечится растяжением альфы
(≤6 → 0, ≥250 → 255), выбросом крапин-артефактов (было 4 штуки) и заливкой RGB
под прозрачным цветом обводки (#FDF8E9). **Пришлют замену — прогнать те же шаги.**

**Как сделана вырезка в блоке «О нас»** (исходник клиента — PNG 4092×6148 БЕЗ альфы,
фон чёрный, обводка обрезана верхним краем кадра: на y=0 уже крем, волосы
с y=72). Скрипт-однодневка на numpy + scipy.ndimage + Pillow:
1. силуэт = `luma > 30` + `binary_fill_holes`;
2. **старая обводка снимается по цвету** (`R>230, G>205, B>180, 18<R−B<70, 6<R−G<40`) —
   белая футболка и кроссовки под условие `R−B>18` не попадают, а кремовые блики
   внутри фигуры становятся дырками и возвращаются заливкой;
3. холст добивается сверху, обводка рисуется заново по расстоянию до силуэта
   (`distance_transform_edt`, ширина замерена по 98-му процентилю = **93.8px**) —
   над головой шапочка получается круглой сама собой;
4. экспорт 1100×2800 WebP q82, 186 КБ.
Пересобирать вырезку → повторить эти шаги, иначе обводка снова будет рваной.

## ⚠️ Что осталось от офлайн-школы и требует данных клиента

Клиент решил (12.08.2026) домен, телефон, CRM-форму и счётчики **скопировать как
есть**. Это осознанная временная мера, но перед запуском их надо заменить:

1. **Домен** — `agb.lancmanschool.com` во всех абсолютных URL (canonical, `og:url`,
   `og:image`, `robots.txt`, `sitemap.xml`, JSON-LD). Сейчас совпадает с офлайн-сайтом,
   то есть две разные страницы канонизируются на один адрес.
2. **CRM-форма Битрикс24** — `click/225/yn061r` + `loader_225.js`. Это **московская**
   форма: в ней выбор филиала из трёх адресов, для онлайн-школы бессмысленный.
   Номер стоит в разметке в ДВУХ местах — у `<script>`-лоадера и у скрытой
   `.b24-trigger`; в лоадере ещё и номер `loader_NNN.js`.
3. **Телефон** `+7 (499) 346-73-50` в шапке, бургер-меню и футере.
4. **Счётчики** Roistat (`4be4190e…`) и Яндекс.Метрика (`37734395`) — общие с офлайн-сайтом,
   поэтому статистика двух сайтов сейчас складывается в одну. ℹ️ Roistat ещё и
   **подменяет номер телефона на странице** (call tracking): в разметке 499, в браузере
   отображается 8 (495) 241-22-73 — это не ошибка вёрстки.
5. **Email в футере не заведён** — реального адреса у нас не было, выдумывать не стали.
   Каналы связи в футере (Telegram, WhatsApp, ВКонтакте) — настоящие, взяты из
   офлайн-проекта.

⚠️ **`.vscode/sftp.json`: `uploadOnSave` выключен намеренно.** В файле лежат доступы
**офлайн-сайта**, и с включённой заливкой первое же сохранение затёрло бы живой
agb.lancmanschool.com онлайн-версией. Получите креды площадки онлайн-школы,
подставьте их и только тогда возвращайте `uploadOnSave: true`.

---

← [CLAUDE.md](../CLAUDE.md) · [README.md](../README.md)
